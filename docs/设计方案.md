# 个人 Agent 平台 · 完整设计方案

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 状态 | 定稿，作为 M1 开发依据 |
| 定位 | 单用户、长期运行的个人 Agent 平台（非生产级，但工程级） |

---

## 1. 项目定位

### 1.1 一句话定位

一个跑在个人 Mac 上的 Agent 平台：**以 Loop 引擎为核心调度中枢，通过注册进来的 MCP、工具、插件、Skill 完成明确的（非编程类）工作任务，知识库提供检索支撑，Web 界面使用**。

### 1.2 核心场景

- 日常工作任务处理（明确目标的任务，由 Agent 循环调用能力完成）
- 个人知识库构建与检索（RAG）
- 技术细节验证（平台本身也是试验场）
- 长期无人值守任务（定时触发 + 断点恢复）

### 1.3 设计原则（六条）

1. **循环要薄、上下文要管、经验要沉淀**——业界五个成熟系统（Claude Code / OpenClaw / dsh / Hermes / Kimi）的最大共识
2. **依赖边界 = 进程边界**——凡膨胀的、不确定的、第三方的依赖，一律推出核心进程，用协议（MCP/HTTP）通信
3. **一库多角色**——PostgreSQL 承担业务数据、向量库、能力索引、检查点、任务队列五种角色，不引入额外存储
4. **配置即代码**——compose 文件、初始化脚本、迁移全部入 git，任何时候一键重建
5. **概念收敛**——Tool / Skill / MCP / 插件统一为"能力"（Capability），一套注册中心、一套发现机制管到底
6. **确定性闸门**——LLM 的不确定性用确定性机制驯服：预算熔断、注册测试、人工确认、验证回环

### 1.4 与企业级方案（AgentForge）的差异声明

本平台不做：多用户/组织权限体系、双向安全网关、算力调度、工业协议对接（Fusion Gateway）、外部工作流引擎（Dify）。但吸收其：记忆整理 Job、入参确认卡片、注册即测试、上下文压缩保护名单、闹钟/定时任务分离等机制。

---

## 2. 总体架构

```
┌────────────────────── Web 前端（后置开发，OpenAPI 契约驱动）──────────────────────┐
│  对话/任务界面（思考过程+计划进度可见）│ 能力/知识库/模型管理 │ 确认卡片 │ 通知中心  │
└────────────────────────────────────┬────────────────────────────────────────────┘
                                     │ REST + SSE
┌────────────────────────────────────▼────────────────────────────────────────────┐
│ API 进程（第一期，uvicorn workers=1）                                              │
│                                                                                    │
│  api/v1  ── auth │ agents │ conversations │ runs │ knowledge │ capabilities │ ...│
│                                                                                    │
│  ┌─ engine（Loop 引擎）────────────────────────────────────────────────────┐    │
│  │  inbox（事件队列）→ intent_router → assembler → ReAct回环 → verify       │    │
│  │  planner（计划外置）  hooks（生命周期钩子）  budget（预算熔断）            │    │
│  │  checkpointer（PostgresSaver）  events（SSE流）                          │    │
│  └────────────────────────────────┬────────────────────────────────────────┘    │
│  ┌─ discovery（能力发现与装配）────┐  ┌─ capabilities（能力注册中心）─────────┐    │
│  │  indexer │ retriever │          │  │  registry（注册+冒烟测试）            │    │
│  │  assembler（上下文装配+压缩）    │  │  mcp_client（连接管理）               │    │
│  └────────────────────────────────┘  └────────────────────────────────────────┘    │
│  ┌─ skills_forge（技能沉淀闭环）──┐  ┌─ memory（记忆体系）────────────────────┐   │
│  └────────────────────────────────┘  └────────────────────────────────────────┘   │
│  knowledge（解析/切分/检索管道）  models（Provider管理+token记账）                  │
│  scheduler（定时任务/闹钟/记忆整理Job）  files  runs  conversations  agents  auth   │
└──────┬──────────────────┬──────────────────┬──────────────────────────────────────┘
       │                  │ MCP/HTTP         │ SQL
       │                  ▼                  ▼
┌──────────────┐  ┌──────────────────────────────┐  ┌─────────────────────────────┐
│ PostgreSQL   │  │ 能力进程群（容器/独立进程）    │  │ 模型层（API 远程 / Ollama） │
│ 业务表       │  │  MCP Server × N              │  │  LLM / Embedding Provider   │
│ pgvector     │  │  自研插件 × N（即自写MCP）    │  │  （统一 Provider 接口）     │
│ 能力索引     │  │  解析服务容器（docling等）     │  └─────────────────────────────┘
│ 检查点       │  └──────────────────────────────┘
│ 任务队列     │
└──────────────┘
```

**关键架构判断：执行主体是 LLM 驱动的 Loop，确定性代码（MCP/插件）是 Loop 调用的工具。** 这与 AgentForge"workflow 为主体、LLM 做调度"相反，智能上限更高，因此对驯服不确定性的机制（预算/测试/确认/验证）依赖更重——这些机制在本方案中均为一等公民。

---

## 3. 技术选型

| 层 | 选型 | 理由 | 落选者 |
|---|---|---|---|
| 编排框架 | **LangGraph**（1.0.x） | Agent Loop 工程化事实标准：状态图/循环/持久化/人工介入/流式开箱即用 | 自研循环（重复造轮）、dsh 式可换循环（过度工程） |
| 后端 | **Python 3.12+ / FastAPI / uv** | AI 生态第一语言；async 原生；uv 依赖管理快且锁死 | Node/Go（AI 生态弱） |
| ORM/迁移 | **SQLAlchemy 2.0 (async) + Alembic** | 成熟、异步、迁移可控 | — |
| 数据库 | **PostgreSQL 17 + pgvector** | 一库多角色；个人规模向量性能足够 | Milvus/Qdrant（运维成本）、Redis（一期能省） |
| 能力协议 | **MCP**（langchain-mcp-adapters） | 事实标准；自研插件复用同通道 | 私有插件协议 |
| 检查点 | **PostgresSaver** | LangGraph 官方组件，与主库同库 | SQLite（弱）、Redis（多余） |
| 容器运行时 | **OrbStack**（已装） | macOS 性能最优 | Docker Desktop（重）、Colima（无GUI不重要但生态弱） |
| 前端 | 后置（React/Vue 届时定） | 契约先行：OpenAPI + SSE | — |
| 调度 | **APScheduler** | 进程内定时，个人规模足够 | Celery（重） |

---

## 4. 数据层设计

### 4.1 PostgreSQL 的五种角色

业务数据 / 向量库（pgvector）/ 能力语义索引 / LangGraph 检查点 / 任务队列（runs 状态机）。**单实例、单库 `agent_platform`，数据卷映射宿主机 `./data/postgres`。**

### 4.2 核心表清单

| 表 | 职责 | 关键字段 |
|---|---|---|
| `users` | 极简单用户 | id, name, password_hash, api_token |
| `agents` | Agent 定义 | id, name, avatar, soul_md, identity_md, memory_md, system_prompt, model_provider_id, tool_budget(单轮工具上限), enabled |
| `conversations` | 会话（= LangGraph thread） | id, agent_id, title, status, last_active_at |
| `messages` | 消息流 | id, conversation_id, role, content(JSONB), token_usage, created_at |
| `runs` | **任务队列/状态机** | id, conversation_id, agent_id, trigger(手动/定时/闹钟), status(pending/running/paused_awaiting_confirm/failed/done/cancelled), budget(jsonb: max_iters/token上限/超时), budget_used, input(JSONB), result(JSONB), checkpoint_ref, timestamps |
| `run_events` | 执行事件流（审计+SSE回放源） | id, run_id, type, payload(JSONB), created_at |
| `plans` | 计划外置（todo） | id, run_id, items(JSONB: [{text,status}]), updated_at |
| `capabilities` | **统一能力表** | id, type(tool/skill/mcp/plugin), category(external/internal), name, description, payload(JSONB: launch配置/SKILL.md/函数签名), embedding(vector), test_info(JSONB: 输入输出对), enabled, health_status, version |
| `capability_bindings` | Agent↔能力绑定 | agent_id, capability_id, mode(pinned/semantic) |
| `kb_folders` / `kb_docs` / `kb_chunks` | 知识库（**目录树管理**：产品知识/研发知识/生活知识等） | folders 含 parent_id、path 物化路径；chunks 含 content, embedding(vector), embedding_model, dim |

> 完整字段级设计见 [数据库设计.md](./数据库设计.md)（主数据 9 表 + 业务数据 14 表 + 框架表 3 张）。
| `model_providers` | 模型接入 | id, kind(llm/embedding), provider, base_url, api_key(加密), model_name, params(JSONB), limits(JSONB) |
| `model_usage_daily` | 按模型按天记账 | provider_id, date, input_tokens, output_tokens |
| `memory_files` | 日期记忆（情景记忆载体） | id, kind(platform/daily), date, content(md), embedding |
| `skill_proposals` | 技能沉淀草稿 | id, run_id(来源), draft_md, status(proposed/approved/rejected), review_note |
| `timers` | 定时任务（执行业务） | id, cron, agent_id, input_template, next_fire_at, status |
| `alarms` | 闹钟（纯提醒） | id, fire_at, content, url, status |

### 4.3 记忆五层映射（借鉴 Hermes 分层 + AgentForge 机制）

| 记忆层 | 内容 | 载体 |
|---|---|---|
| 工作记忆 | 当前任务上下文 | LangGraph State + 检查点 |
| 情景记忆 | 做过什么、结果 | runs + memory_files(daily) |
| 程序记忆 | Skill 库 | capabilities(type=skill) |
| 语义记忆 | 知识资产 | kb_chunks 向量 |
| 用户画像 | 偏好、习惯、纠正史 | memory_files(platform) + profiles 内容 |

**每日记忆整理 Job**（凌晨 3:00，scheduler 内置任务）：回顾当日会话 → 平台级要点写入 `memory_files(platform)`；个人关键信息写入 `memory_files(daily, YYYY-MM-DD)`。会话边界以凌晨 3 点切割（借鉴 AgentForge）。

---

## 5. 后端模块设计

### 5.1 目录结构

```
backend/
├── pyproject.toml              # uv 管理，依赖白名单（见 §7.3）
├── .env                        # 本地配置（gitignore）
├── alembic/                    # 迁移
├── app/
│   ├── main.py                 # FastAPI 入口 + lifespan（初始化 MCP 池/调度器/整理Job）
│   ├── core/                   # config / logging / errors
│   ├── db/                     # session / base
│   ├── modules/
│   │   ├── auth/               # 单用户：登录→JWT、get_current_user
│   │   ├── agents/             # Agent CRUD + 人格文件组织
│   │   ├── conversations/      # 会话与消息
│   │   ├── runs/               # 队列状态机、历史、恢复/终止
│   │   ├── engine/             # ★ Loop 引擎
│   │   │   ├── inbox.py        # 事件队列（用户输入/中断/恢复=队列操作）
│   │   │   ├── intent_router.py# 闲聊/工作快速分流
│   │   │   ├── graph.py        # LangGraph 图构建（含验证节点、interrupt点）
│   │   │   ├── planner.py      # 计划外置（todo 入库 + plan_updated 事件）
│   │   │   ├── hooks.py        # 生命周期钩子（on_turn_start/end、on_tool_call 前后、on_context_pressure）
│   │   │   ├── budget.py       # 预算熔断（改为钩子实现）
│   │   │   ├── checkpointer.py # PostgresSaver 装配
│   │   │   ├── backend.py      # EngineBackend 协议（二期拆 worker 的接缝）
│   │   │   └── events.py       # SSE 事件封装
│   │   ├── capabilities/       # 能力注册中心
│   │   │   ├── registry.py     # CRUD + 热注册/热摘除 + 注册冒烟测试(test_info)
│   │   │   ├── mcp_client.py   # MCP 连接管理（stdio/http）
│   │   │   └── builtin_tools.py# load_skill / search_more_tools / search_knowledge
│   │   ├── discovery/          # 能力发现与装配
│   │   │   ├── indexer.py      # 能力描述→embedding→入库
│   │   │   ├── retriever.py    # 任务→Top-K 能力
│   │   │   └── assembler.py    # 上下文装配（token预算分配 + 两级压缩）
│   │   ├── skills_forge/       # 技能沉淀闭环（复盘→草稿→人审→入库→索引）
│   │   ├── knowledge/          # pipeline(解析调容器) / retriever / router
│   │   ├── memory/             # profiles / 日期记忆 / 整理Job逻辑
│   │   ├── models/             # Provider 管理 + token 记账
│   │   ├── files/              # 本地文件存取
│   │   └── scheduler/          # timer_tasks / alarms / memory_consolidation
│   └── api/v1.py               # 路由聚合
└── tests/
```

### 5.2 模块依赖规则（唯一方向）

```
api → modules → db/core
engine → agents, capabilities, discovery, models, knowledge(retriever)
其余领域模块互不 import；横切关注点（审计/计量/熔断）一律挂 engine hooks，不侵入各模块
```

---

## 6. 核心机制设计

### 6.1 Loop 引擎：图结构与执行流

```
START
  → intent_router        闲聊→廉价模型直达；工作→继续
  → context_assembly     见 §6.2 发现与装配
  → planner              生成计划（todo 入 plans 表，不塞上下文）
  → agent ⇄ tools        ReAct 回环（LangGraph 条件边）
  → verify               验证节点：达标→END；不达标→带反馈回 agent（计数）
  → interrupt 确认点     任务提交前 / 高危工具调用前 → confirmation_request
  → END（或 预算熔断 / 转人工）
```

- **inbox 队列化**（借鉴 dsh）：循环消费事件队列而非直接读消息数组；用户输入、定时触发、人工中断、恢复指令统一为队列事件 → 分叉/恢复/回放天然一致，且为二期拆 worker 预留接缝
- **检查点**：每超步自动落 PostgresSaver；进程崩溃/重启后从断点续跑
- **计划外置**（借鉴 Claude Code + Kimi）：长任务计划存 `plans` 表，Agent 每轮重读计划状态；前端渲染 todo 进度（plan_updated 事件）
- **预算熔断**（钩子实现）：max_iterations 硬上限 / 单任务 token 成本上限 / 同工具同参数连续 N 次循环检测 / 全局超时
- **结构化错误**：工具异常返回结构化错误对象给模型，不抛堆栈

### 6.2 能力体系：统一模型、注册、发现、装配

**统一能力模型**：tool（函数）/ skill（Markdown 指令书+可选脚本，SKILL.md 惯例，yaml头 name/description）/ mcp（Server，含工具开关）/ plugin（自研 MCP Server）——四种 type 一张表。Skill 与 Tool 正交：Tool 是手，Skill 是操作手册。

**注册流程**：提交元数据 → 规范校验 → 按 `test_info` 跑冒烟测试（借鉴 AgentForge TEST.md）→ 通过才注册生效 → 自动 embedding 入能力索引。注册中心管理 MCP 连接生命周期（stdio/http，热注册/热摘除/健康检查）。

**发现机制三层**：

1. 静态绑定（pinned）：Agent 显式绑定常驻能力（高频 5–10 个）
2. **语义发现（主力）**：任务描述 → embedding → 能力索引 Top-K → 动态装配（控制上下文膨胀的核心手段）
3. Skill 渐进披露：初始只注入 ~100 token 元数据，Agent 按需 `load_skill(name)` 拉取全文

**上下文装配器**（discovery/assembler）：

- token 预算分配：人格/画像（protected）｜技能元数据｜候选工具描述｜对话历史｜计划状态
- **保护名单**（借鉴 AgentForge）：人格文件、用户画像、活跃计划永不压缩
- 两级压缩（借鉴 Claude Code 五级压缩的简化）：① 裁剪折叠旧工具观察结果 → ② 全量摘要重启
- 触发条件：上下文使用率 ≥ 80%

**内置元工具**：`load_skill` / `search_more_tools`（检索即工具化）/ `search_knowledge`（知识库对引擎而言只是一个工具）

### 6.3 技能沉淀闭环（skills_forge，借鉴 OpenClaw + Hermes）

```
任务成功或被纠正
  → 复盘节点："本次方法是否值得固化为 Skill？"
  → 生成 SKILL.md 草稿 → skill_proposals 表
  → 人工审核（Web 界面确认/驳回/编辑）
  → 入库（capabilities type=skill）→ 自动 embedding 进索引
  → 下次同类任务被语义发现命中
```

这是"越用越聪明"的实现路径（L4 爬山循环的第一步），也是平台随使用增值的核心抓手。任务失败/被纠正同样有沉淀价值（"别这么做"也写入）。

### 6.4 人工介入（借鉴 AgentForge 确认卡片）

- 确认点：任务单提交前（实体抽取结果打包确认，模型提取可能出错）；高危工具调用前（写操作/外部发送类）
- SSE `confirmation_request` 事件 → 前端渲染参数确认卡片（已解析参数自动填充）→ 用户确认/修改 → 恢复执行
- 配合检查点，暂停可跨小时/天

### 6.5 触发器体系（scheduler）

| 类型 | 行为 | 场景 |
|---|---|---|
| 手动 | 用户发消息 → run | 日常 |
| 定时任务 timer | cron 触发 → 生成任务单 → run | 周期性工作 |
| 闹钟 alarm | 到点纯提醒，不执行业务 | 提醒类 |
| 记忆整理 Job | 每日 03:00 | 记忆巩固 |

### 6.6 通知路由（借鉴 AgentForge）

run 完成时：若当前活跃会话与该任务相关 → 实时推送；否则进通知中心/次日摘要。

### 6.7 SSE 事件类型清单

`message_delta`（token 流）/ `thought`（思考）/ `tool_call` / `tool_result` / `plan_updated` / `confirmation_request` / `budget_warning` / `run_status` / `error`

---

## 7. 进程与部署模型

### 7.1 一期：单进程 + 四条硬纪律

```
[API 进程 uvicorn workers=1]              [独立容器/进程群]
  FastAPI + LangGraph engine                postgres:17+pgvector
  APScheduler + MCP 连接池                  解析服务（docling，REST）
  （纯 async I/O，依赖白名单）               MCP Server × N / 自研插件 × N
                                            （可选）Ollama
```

**纪律**：① 核心进程禁止同步阻塞操作（同步一律 `asyncio.to_thread` 或外推）；② workers=1（进程内单例状态：MCP 池/调度器）；③ 引擎调用走 `EngineBackend` 协议；④ runs 表从第一天就是队列/状态机语义。

**看门狗**：OrbStack `restart: always` + checkpointer 恢复 = 进程级自愈。

### 7.2 二期拆分触发条件（对号入座，不拍脑袋）

- 定时长任务成为常态（每日有 >10 分钟级自动 Loop）
- 核心代码频繁升级，重启打断任务变多
- 进程 RSS 持续增长、周均需手动重启
- 需要多 Loop 真正并行互不干扰

拆分方式：Engine Worker 独立进程，API↔Worker 用 PostgreSQL（runs 表 + LISTEN/NOTIFY）作队列，不引入 Redis。

### 7.3 依赖白名单（核心进程）

```
fastapi, uvicorn, langgraph, langchain-core,
langchain-openai(或其他provider包), langchain-mcp-adapters,
langchain-postgres, sqlalchemy[asyncio], asyncpg, alembic,
apscheduler, httpx, pydantic, pydantic-settings, pyjwt, passlib
```

**准入审查**：任何新依赖先问"能否推到独立进程"。解析（unstructured/docling）、本地推理（torch）等重依赖永远在容器隔离区。

### 7.4 docker-compose 服务清单

| 服务 | 镜像 | 说明 |
|---|---|---|
| postgres | pgvector/pgvector:pg17 | 数据卷 → ./data/postgres；restart: always |
| parser | docling 或 unstructured 服务镜像 | 按需启动，REST 接口 |
| api（可选容器化） | 自建 | 一期可直接 uv 跑宿主机，二期容器化 |
| 各 MCP Server | 社区镜像/自建 | 按注册表配置拉起 |

### 7.5 备份策略

- 每日 `pg_dump` → `./backup/`（保留 14 天，launchd 或容器 cron）
- 升级核心前强制备份；备份目录 gitignore

---

## 8. API 设计（v1 草案）

```
POST /api/v1/auth/login

CRUD /api/v1/agents
GET/PUT /api/v1/agents/{id}/capabilities          # 绑定管理(pinned/semantic)

GET  /api/v1/conversations
POST /api/v1/conversations/{id}/messages          # 发消息 → 触发 run

GET  /api/v1/runs                                 # 历史（过滤：状态/会话/触发方式）
GET  /api/v1/runs/{id}                            # 详情
GET  /api/v1/runs/{id}/events                     # SSE 流（实时 + 回放）
POST /api/v1/runs/{id}/resume | /abort | /confirm # 恢复/终止/确认卡片提交

CRUD /api/v1/capabilities                         # 注册(含冒烟测试)/启停/版本
POST /api/v1/capabilities/{id}/test               # 手动重跑测试
GET  /api/v1/capabilities/search?q=               # 语义搜索预览

GET  /api/v1/skill-proposals                      # 技能草稿审核队列
POST /api/v1/skill-proposals/{id}/approve | /reject

CRUD /api/v1/knowledge/bases | /docs
POST /api/v1/knowledge/docs:upload | {id}:reindex

CRUD /api/v1/model-providers
GET  /api/v1/model-usage?group_by=model,date

CRUD /api/v1/timers | /alarms
GET  /api/v1/memory/files?kind=&date=
GET  /api/v1/notifications
```

`/docs` 自动生成的 OpenAPI 即前后端契约。CORS 面向本地前端开发端口。

---

## 9. 治理与安全（个人级）

| 关注点 | 机制 |
|---|---|
| 费用失控 | 预算熔断钩子 + model_usage_daily 记账 + provider 级日限额 |
| 死循环 | 循环检测 + max_iterations + 全局超时 |
| 错误实体 | 任务提交前确认卡片 |
| 高危操作 | 高危工具调用前确认（capability 加 `risk_level` 标记） |
| 能力质量 | 注册即测试 + 健康检查 |
| 密钥 | api_key 加密落库，.env 不入 git |
| 审计 | run_events 全量留痕，可回放 |

---

## 10. 里程碑

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M1 地基** | 项目骨架 + docker-compose(PG) + 迁移 + auth + agents CRUD + OpenAPI | 表结构迁移成功，API 可调 |
| **M2 引擎** | engine 全量（inbox/router/graph/planner/hooks/budget/checkpointer/SSE）+ 1 个内置工具 | curl 跑通完整 ReAct 循环；kill 进程后断点恢复成功；SSE 可观察 |
| **M3 能力** | capabilities + discovery + MCP 接入 | 注册一个真实 MCP Server（含冒烟测试），Agent 经语义发现调用它完成任务；工具开关生效 |
| **M4 知识** | knowledge 管道 + 检索工具 + 解析容器 | 上传 PDF → 切分入库 → 对话中命中检索 |
| **M5 记忆与触发** | scheduler（定时/闹钟/整理Job）+ memory 分层 + 通知路由 | 定时任务自动跑并出结果；隔日会话能引用前日记忆 |
| **M6 沉淀** | skills_forge + 确认卡片 + 前端一期 | 任务成功后产出 Skill 草稿→人审入库→二次任务命中复用 |

前端在 M2 后可并行介入（契约已冻结于 M1/M2 的 OpenAPI）。

---

## 11. 演进路径与红线

### 11.1 明确不做（本期红线）

- 多用户/组织/权限体系
- 自修改软件（Agent 改平台自身代码）
- 多 Agent Swarm（>3 并行子 Agent）
- 消息平台网关（微信/TG 接入）——留作扩展点
- 内核级沙箱（容器隔离已覆盖个人威胁模型）

### 11.2 预留的演进方向

| 方向 | 接缝 |
|---|---|
| 拆 Engine Worker 进程 | EngineBackend 协议 + runs 队列语义 |
| 多 Agent 协作 | LangGraph 子图 + Supervisor 模式，agents 表已有多行能力 |
| 消息网关接入 | inbox 事件队列——网关只是新的生产者 |
| 换 LLM/Embedding 供应商 | Provider 接口 + 向量表带 model 标记（触发重建索引） |
| 多用户（若真需要） | auth 单点改造 + 全表加 user_id 的迁移成本 |

---

## 12. 关键设计决策记录（ADR 摘要）

| # | 决策 | 理由 | 落选方案 |
|---|---|---|---|
| 1 | LangGraph 而非自研循环 | 循环/持久化/介入/流式全是刚需，自研无收益 | 手写 while 循环 |
| 2 | PostgreSQL 单库五角色 | 个人规模下运维成本最低，LangGraph 官方同款 checkpointer | PG+Redis+Milvus 组合 |
| 3 | 能力统一为一张表 | 概念收敛，注册/发现/开关一套机制 | MCP/Tools 分池（AgentForge式，企业需要，个人不需要） |
| 4 | 单进程起步 | 依赖已外推、检查点兜底；拆分条件明确 | 一开始就多进程（复杂度前置） |
| 5 | 计划外置 | 长任务不依赖上下文记性 | 计划塞上下文 |
| 6 | 技能沉淀带人工审核闸门 | 自动写技能有污染风险；人审成本极低 | 全自动沉淀 |
| 7 | 语义发现为主、静态绑定为辅 | 上下文膨胀是能力增长的第一死因 | 全量工具塞 prompt |
| 8 | 前端后置、契约先行 | 后端选型不受前端绑架，OpenAPI 冻结后并行开发 | 前后端同步开发 |
| 9 | 密码哈希直接用 bcrypt 库 | passlib 已停止维护且与 bcrypt>=4 存在兼容问题，功能上只需 hash/verify 两个函数（M1 实施时记） | passlib |
