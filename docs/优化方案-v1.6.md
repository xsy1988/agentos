# Agent 平台 v1.6 优化方案

> 迭代代号：**v1.6 / M9**　·　基线：`3fd07d0`（tag `v1.5.0`）　·　数据快照：2026-09-17
> 复现基线：`uv run pytest -q` → 130 passed　·　全部证据的复现命令见 §13。

---

## §0 文档定位与证据标记法

### 0.1 本文是什么

本文是 v1.6 迭代的**唯一实施依据**：把平台当前存在的结构性问题，逐条落成可执行的改造规格（目标 / 现状证据 / 变更点 / DDL / 事件 schema / API / 迁移 / 测试 / 风险 / DoD）。

### 0.2 本文不是什么

| 不做 | 原因 |
|---|---|
| 不是重新设计方案 | `docs/设计方案.md` 的七条设计原则（§1.3）与边界声明（§1.4）**不变**；本文所有改造都在其内 |
| 不是新增子系统 | 不引入 Redis / MQ / 独立队列服务；`runs` + `inbox_events` + `run_events` 三张表仍是全部协同载体（§7.1 纪律②：workers=1） |
| 不是多用户权限体系改造 | `docs/设计方案.md` §1.4 明确不做；本文只补**已有的鉴权边界**在新增通道上的落点 |
| 不是时间表 | 全文不含工期估算；里程碑只给**依赖顺序与 DoD** |

### 0.3 与前一份方案的关系

用户提供的 `Agent平台优化方案-完整版.md`（下称**原方案**）作为输入使用。本文**继承其骨架**（五条不变量、Q1–Q7 编号、P0/P1/P2 分级、反模式清单），但对其**事实审计部分逐条复核并修正**：原方案有 6 处事实错误、2 处结论过时（详见 §1.4），若原样实施会导致方向性偏差。

### 0.4 证据标记法

| 标记 | 含义 | 可复现性 |
|---|---|---|
| `[实测]` | 对运行库 / 执行命令的直接观测 | 附快照时间与命令（§13），数字会随使用漂移 |
| `[代码]` | 源码事实，附 `文件:行` | 与 HEAD `3fd07d0` 绑定 |
| `[推断]` | 由 `[实测]`/`[代码]` 推导的结论，未经独立验证 | 需在实施时验证 |
| `[已修复]` | 原方案指出的缺陷在当前 HEAD **已不存在**，只保留回归测试 | — |

**所有 `[实测]` 数字都在 §13 给出可复跑命令**，且不作为恒定事实引用。

---

## §1 结论摘要

### 1.1 三句话结论

1. 原方案**方向正确、事实有误**：平台并不缺"等待能力"的机制（`interrupt` + PG 检查点 + `inbox_events` 恢复 + 支线收敛**都已存在且在用**），真正的断裂点是**"全局超时"没有账本**——`runs.started_at` 一个字段都没写过，超时计时按"段"重置，导致一次 1401 秒的执行在最后 600 秒被判死、`result=NULL`、全部成果丢弃 `[实测][代码]`。
2. 平台的**能力可见性缺陷被原方案低估**：`tool_budget` 只有 8 和 6，而单个 `filesystem` MCP 就展开 **14** 个工具，装配末尾是**硬切片** `tools[:tool_budget]` `[实测][代码]`——这不是"可能不进列表"，而是**结构性算术超约束**，Worker 声明的核心工具必然被截断。
3. v1.6 的主线是**三件事**：让运行时长可测且超时真正全局（P0-1）、让能力可见性成为硬保证（P0-2）、让失败与结果成为结构化一等对象（P0-3/P0-5）。await 一等化（P0-4）与 inbox 契约闭合（P0-6）是随之的必要配套。

### 1.2 问题总表

| 编号 | 问题 | 严重度 | 证据 | 直接后果 |
|---|---|---|---|---|
| **Q-01** | `runs.started_at` 从未写入（27/27 为 NULL），平台没有任何运行时长账本 | 🔴 P0 | `[实测]` `[代码]` | 运行时长不可测、SLA 不可算、原方案的"P95 时长判据"在平台内**不可执行** |
| **Q-02** | 全局超时按"段"重置：每次 interrupt/resume 重新 `wait_for(..., timeout=600)` | 🔴 P0 | `[实测]` `[代码]` | 实测 run 存活 1401s（预算 2.3×）；超时后 `result=NULL`，全部中间成果丢弃 |
| **Q-03** | 能力可见性无硬保证：`tool_budget` 8/6 vs 单个 MCP 展开 14 工具，装配末尾硬切片 | 🔴 P0 | `[实测]` `[代码]` | Worker 核心工具静默丢失，任务空转且**无任何提示** |
| **Q-04** | 失败被降级为自然语言：`httpx.HTTPError` → 一句中文文本，模型无法区分"失败"与"结果" | 🔴 P0 | `[代码]` | **假完成**的根因；`error` 事件全库仅 1 条 |
| **Q-05** | 结果为非结构化单字段 `{"text": ...}`，无结果卡、无 artifact、无历史重放 | 🔴 P0 | `[代码]` `[实测]` | 结果不可机读、不可复用；失败 run 结果直接丢失 |
| **Q-06** | `inbox_events` 声明 5 类、实现 3 类、**无 `else` 兜底** | 🟠 P0 | `[代码]` `[实测]` | 未知类型静默丢弃；`resume` / `capability_changed` 从未落库 |
| **Q-07** | 事件类型三处注册表漂移（后端 `EVENT_TYPES` 10 类 / 实际发射 11 类 / 前端消费 12 类） | 🟠 P0 | `[实测]` `[代码]` | `context_compacted` 在产线使用却**不在冻结契约内**；前端 2 个分支永不触发 |
| **Q-08** | 预算 token 闸永不生效：`max_tokens_per_run` 全 NULL → `or 0` → 不限 | 🟠 P0 | `[实测]` `[代码]` | 实测单 run 输入 57,788 tokens 无任何闸门拦截 |
| **Q-09** | 工具执行无上下文：builtin 分支恒 `ok=True`，mcp 分支 `ok=True` 硬编码 | 🟠 P1 | `[代码]` | 工具失败无法上报；幂等键、run_id 传递通道不存在 |
| **Q-10** | 同轮 `search_more_tools` 缓存互相覆盖（`updated_cache` 单变量 + 整体 cache 语义） | 🟡 P1 | `[代码]` | 同轮第二次搜索的结果会丢掉第一次的工具 |
| **Q-11** | `provider.limits` 表已留位、语义为零（9 个 provider 全为 `{}`） | 🟡 P1 | `[实测]` | 无限流执行 |
| **Q-12** | 记账口径失真：仅 `status=="done"` 计 `run_count`，token 无论成败都累 | 🟡 P1 | `[代码]` | 队列/成本报表不可信 |
| **Q-13** | 无界并发 `asyncio.create_task`，无信号量 | 🟡 P1 | `[代码]` | 与 MCP 单连接锁负向叠加，长调用拖垮探活 |
| **Q-14** | 前端 plugin 侧边栏与任务详情**互斥**（有 sidebar 即整体替换） | 🟡 P1 | `[代码]` | 分钟级场景下"等解析"与"处理决策"不可同屏 |
| **Q-15** | 7 个 `blocked` 支线由**人工 SQL 清洗**产生（note 后缀不在代码里） | ⚪ P2 | `[实测]` `[代码]` | 无前台收敛路径，运维手动介入不可持续 |
| **Q-16** | 外部服务无法取件：`files` 上传/取件接口均强制用户 JWT | ⚪ P2 | `[代码]` | 文件通道对无用户凭据的外部 Worker 存在断点 |

**问题 → 章节映射**（每项都有归口，无遗漏）：

| 章节 | 覆盖 |
|---|---|
| §4 P0-1 | Q-01、Q-02 |
| §4 P0-2 | Q-03 |
| §4 P0-3 | Q-04、Q-09（失败语义部分） |
| §4 P0-4 | Q-02（等待不计入执行预算）、Q-06（`resume` 处置） |
| §4 P0-5 | Q-05 |
| §4 P0-6 | Q-06、Q-07 |
| §5 P1-1 | Q-13 |
| §5 P1-2 | Q-08、Q-11 |
| §5 P1-3 | Q-12 |
| §5 P1-6 | Q-15 |
| §5 P1-7 | Q-10 |
| §5 P1-8 | Q-14 |
| §5 P1-10 | Q-09（上下文传递部分） |
| §6 P2-1 | Q-16 |
| §6 P2-3 | Q-15（前台化 + 规范） |

### 1.3 与原方案的差异修正表

| # | 原方案断言 | 复核结果 | 判定 |
|---|---|---|---|
| 1 | 缺陷 A「等待（await）不是一等能力 ★」 | 平台**已有**：`interrupt` + PG 检查点 + `_resume_run` 的 `Command(resume=...)` + `inbox_events` 恢复 + `_reconcile_awaiting_steps`→`reconcile_orphaned_awaits` 支线自动收敛 `[代码]` | ⚠️ **措辞过重**。真正缺的是**时长账本**（Q-01/Q-02），不是机制 |
| 2 | §3.4「事件类型 9 类在用；`context_assembly`/`context_compacted`/`budget_warning` 从未触发」 | 运行库实测 **10 类在用**，其中 `context_compacted` **已触发 1 次**；`context_assembly` 根本不是事件类型（是**图节点名** `graph.py:1048`），只是前端 `MessageStream.tsx:492` 有一个永不命中的分支 | ❌ **事实错误**，且掩盖了 Q-07 的真实问题 |
| 3 | §7 Q7「判据是 `runs.started_at/finished_at` 的 P95」 | `started_at` 27/27 为 NULL | ❌ **判据在平台内不可执行**；须先修 Q-01 |
| 4 | §3.4「`capabilities/smoke.py:81-88` 纯前端 plugin 被误判 `enabled=false`」 | `smoke.py` 已用 `mcp_connectable(payload)` 跳过 MCP 冒烟并放行，且有单测 `test_mcp_connectable_skips_frontend_only_plugin` `[已修复]` | ❌ **已修复**，只需回归保护 |
| 5 | 缺陷 E「阻碍与决策无结构化标识」 | `task_steps` 有 `resolution` 结构、有自动收敛路径；缺的是**事件可见性**与**前台收敛入口**。7 个 blocked 的 note 后缀"（存量清洗）"在代码中不存在（模板见 `tasks/service.py:578`，无此后缀）`[实测][代码]` | ⚠️ **部分正确**：问题是可观测性，不是数据结构 |
| 6 | §3.5 Q2「token 闸在 `on_turn_end` 已消耗完才判」 | 正确；且 `max_tokens_per_run` 实测全 NULL → `or 0` → **闸门整体失效**（原方案提及但未升级为 P0 主线） | ✅ 正确但**低估** |
| 7 | §3.6「`files/router.py` 上传与取件均强制用户 JWT」 | 核实：`files/router.py:19` router 级 `Depends(get_current_user)` | ✅ 正确 |
| 8 | 缺陷 B「能力可见性无硬保证 ★★」 | 核实且**低估**：不是"可能不进"，是 `tool_budget=8` vs `filesystem` 单 MCP 展开 14 工具的**结构性超约束** | ✅ 正确但**低估** |
| 9 | 缺陷 D「结果不是一等公民」 | 核实：`_finalize` 写 `run.result = {"text": ...}`；`run_events` 无 `card`/`artifact` 事件 | ✅ 正确 |
| 10 | §0.3 明确不新增（MQ/调度/优先级/Redis） | 与 `docs/设计方案.md` §1.4、§7.1 纪律②一致 | ✅ 正确，本文原样继承 |
| 11 | §1.4 五条不变量 I1–I5 | 方向正确，但 I1「单次派发」缺少**失败语义**约束（见 §3.2 I6） | ⚠️ 需补充 |
| 12 | 缺陷 C「输入契约不存在」 | 本轮**未复核**（涉及 `third_party` front-matter 与预检门，非本次实测范围） | ⏸ 挂起，见 §1.5 |

### 1.4 原方案的 6 处事实错误（供溯源）

第 1.3 节中标记 ❌ 的 4 条 + Q2 低估 + 缺陷 A 措辞过重，构成本文与原方案的**全部事实分歧**。除此之外原方案的事实审计与本文一致。

### 1.5 本轮未复核项（不得当作已确认）

| 项 | 状态 | 处理 |
|---|---|---|
| 原方案缺陷 C「输入契约不存在」 | 未复核 | 归入 P1（§5），实施前须单独核实 |
| `filesystem` 能力 `risk_level=write` 导致只读列表也弹高危卡 | 未复核 | 归入 P2 观察项 |
| `provider.params` 全为 `{"via_gateway": true}` | 未复核 | 与 Q-11 同批核实 |
| 前端 `store/ui.ts` 宽度夹取、`ContextPanel` 互斥的具体行号 | 部分复核 | Q-14 实施时精确化 |

---

## §2 现状事实基线

> 本节是全文的**事实底座**。所有 `[实测]` 数据快照于 2026-09-17，命令见 §13。

### 2.1 数据面实测

| 项 | 实测值 | 含义 |
|---|---|---|
| `runs` 总数 | 27（done 24 / cancelled 2 / failed 1） | 任务量小 |
| `runs.started_at IS NULL` | **27 / 27** | **该字段从未被任何代码写入**（Q-01 的直接证据） |
| `runs` 时长分布（`finished_at - created_at`） | 1401 / 482 / 451 / 306 / 293 / 164 / 137 / 129 / 113 / 101 / 71 / 67 秒 | 用替代字段才可见；`started_at` 路径不可用 |
| `tasks` | active 9 / done 7 = 16 | — |
| `task_steps` | done 6 / pending 5 / **blocked 7** | blocked 占比 39% |
| `capabilities` | 17（tool 13 / mcp 2 / plugin 1 / skill 1），全部 enabled、全部有 embedding | 索引健康 |
| `capability_bindings` | **仅 1 条**（`mode=pinned`） | 无 Worker 的领域能力被 pinned |
| `model_providers` | 9（llm 7 / embedding 2），`limits` 全为 `{}` | Q-11 |
| `agents` | `tool_budget` = 8（小助）/ 6（调研员）；`max_tokens_per_run`、`timeout_seconds` **均为 NULL** | Q-03、Q-08 |
| `inbox_events` | user_input 136 / confirmation 96 / abort 16，全部 consumed | 声明 5 类仅用 3 类（Q-06） |
| `run_events` | message_delta 15996 / run_status 102 / tool_call 98 / tool_result 98 / thought 54 / message_reset 29 / plan_updated 29 / confirmation_request 24 / **error 1** / context_compacted 1 | 无 `card` / `progress` / `artifact`（Q-05/Q-07） |

### 2.2 单 run 尸检：`47d69dcc-8880-4848-860b-ab6e7d84d802`

这是 Q-01/Q-02 的**决定性证据**，构成 P0-1 的全部动因。

| 项 | 值 |
|---|---|
| 终态 | `failed`，`error = {"code": "timeout", "detail": "全局超时"}` |
| 存活时长 | **1401 秒**（配置的 600 秒预算的 2.3 倍） |
| `result` | **NULL**（成果全丢） |
| `budget_used` | `{"iterations": 8, "tool_calls": 8, "input_tokens": 57788, "output_tokens": 27774}` |
| 暂停次数 | 5 次 `confirmation_request` |

**时间线关键段**（`[实测]`）：最后一段 `07:50:44.424 → 08:00:44.428` = **恰好 600.004 秒**触发 `timeout`。即超时不是从 run 创建起算，而是**从本次 `_invoke_and_finalize` 进入起算**——5 次暂停全部重置了计时器。

该段内还观测到：9 次工具调用；重复输出「外部采购 Agent 」；`message_delta` 6918 条中仅 4766 条不重复（**约 31% 是重复流式片段**）。

**代码对齐** `[代码]`：

- `runtime.py:446` — `timeout = (run.budget or {}).get("timeout_seconds") or 600`
- `runtime.py:~464` — `final_state = await asyncio.wait_for(self.graph.ainvoke(input_payload, config), timeout=timeout)`
- `_run` 与 `_resume_run` **各自**调用一次 `_invoke_and_finalize` → 各起一次计时
- `_pause_for_confirmation`（`runtime.py:505-518`）只落 `run.status` 与 `run.budget_used`，**不落任何时间信息**
- `runs/models.py:43-47` 只有 `started_at` / `finished_at`，**无 deadline / elapsed 字段**
- `_set_run_status`（`runtime.py:616-628`）**只在终态写 `finished_at`**，从不写 `started_at`

### 2.3 能力可见性的算术超约束（Q-03）

| 事实 | 证据 |
|---|---|
| `filesystem` 单个 MCP 展开 **14** 个工具 | `[实测]` `capability_tools` |
| Agent `tool_budget` = 8 / 6 | `[实测]` |
| 装配末尾硬切片 | `[代码]` `assembler.py` `return {"tools": tools[:tool_budget] + _meta_tools(), "skills": skills}` |
| pinned 是 **Agent 级**语义 | `[代码]` `retriever.py` `CapabilityBinding.mode == "pinned"` |
| Worker 的 capabilities 只经 `_task_scope` → `domain_ids` 参与重排 | `[代码]` `retriever.py` `order_by_scope` |

**结论** `[推断]`：`8 < 14` 意味着**只要 `filesystem` 在候选集中且排在前列，它单独就能吃掉全部名额**，Worker 领域能力被截断后模型看到的是一个"没有业务能力"的工具列表——**平台侧零提示、零事件、零日志**。这是"任务空转"的直接机制。

### 2.4 失败被降级为文本（Q-04）

`[代码]` `tools_builtin.py:171-190` `_procurement_request`：捕获 `httpx.HTTPError` 后**统一返回一句中文文本**（"采购 Agent 服务不可用（…）"），而不是抛错或返回结构化失败。模型收到的是 `ToolMessage` 里的自然语言，**无法区分"外部服务失败"与"业务结果为这个字符串"** → 模型据此继续推理并"完成"任务 = **假完成**。

同处的 5 个工具 `procurement_trigger/status/decisions/submit_decision/report`（`tools_builtin.py:192-234`）实现的是**阻塞式轮询**：模型必须依次调用 触发 → 查状态 → 查状态 → 取决策 → 取报告 才能完成一件事，正是原方案不变量 I1「单次派发」要避免的形态。

另注：`except httpx.HTTPError` **不覆盖** JSON 解析失败、schema 不匹配等异常，这些会直接冒泡成 run 级 `failed`。

### 2.5 事件与收件箱契约面的三处漂移（Q-06 / Q-07）

| 注册表 | 位置 | 条目 | 漂移 |
|---|---|---|---|
| 后端冻结契约 | `backend.py:12-23` `EVENT_TYPES` | 10 类 | **缺 `context_compacted`**（产线已发射） |
| 实际发射点 | `runtime.py` / `graph.py` / `hooks_impl.py` / `assembler.py` | **11 类** | `context_compacted` 在 `assembler.py:394/416/425` 发射 |
| 前端消费 | `Chat/MessageStream.tsx` / `Runs/RunTimeline.tsx` / `Chat/index.tsx` | **12 类** | 多出 `context_assembly`（**无人发射**）、`interrupt`（**无人发射**） |
| inbox 声明 | `engine/models.py:20` | 5 类（`user_input`/`resume`/`abort`/`confirmation`/`capability_changed`） | — |
| inbox 实现 | `runtime.py:219-241` `_handle` | **3 类**，**无 `else` 兜底** | `resume` / `capability_changed` 从未落库 `[实测]` |

冻结契约测试 `backend/tests/test_engine_frozen.py:27` 断言 `set(EVENT_TYPES) == {...10 类...}`——**该测试不覆盖产线在用的 `context_compacted`**，所以漂移可以长期存活而无人发现。

### 2.6 已可用的既有能力（不重做）

| 能力 | 位置 | 说明 |
|---|---|---|
| 暂停/恢复 | `graph.py` interrupt + `runtime.py` `_resume_run` | 已实现 `Command(resume=answer)`，支持字符串与 `{answer,data,applied}` 结构 |
| 确认守卫 | `runs/router.py` `confirm_run`（`if run.status != "paused_awaiting_confirm": raise 409`）、`_resume_run` 同守卫 | 新增等待状态**必须同步改这两处**，否则静默失效 |
| 支线自动收敛 | `runtime.py` `_reconcile_awaiting_steps` → `tasks/service.py` `reconcile_orphaned_awaits` | 仅在 `failed/cancelled/aborted/timeout` 触发 |
| SSE | `runs/router.py` `stream_run_events` | 断线续传 + 25s keepalive + 终态即结束 |
| 四道闸钩子链 | `hooks_impl.py` | 迭代闸生效；token 闸因配置 NULL 而失效（Q-08） |
| 预算续跑 | `runtime.py` `_build_ctx` + `graph.py`（`budget_state` 回写） | `iterations/input_tokens/output_tokens/tool_calls` 四项可跨重启恢复——**缺的是时间维度** |

### 2.7 基线健康度

| 项 | 值 |
|---|---|
| `uv run pytest -q` | **130 passed**, 3 warnings, 6.56s `[实测]` |
| 测试文件 | 16 个，位于 `backend/tests/` |
| lint | `make lint`（ruff + `mypy app`） |
| 工作区状态 | `git status` 干净（`backend/.venv` 已被 `.gitignore` 忽略） |

**结论**：平台有**可用的回归网**，P0 改造可以安全地"先加测试再改行为"。

---

## §3 目标状态与不变量

### 3.1 目标状态 E1–E8

| 编号 | 目标状态 | 可验证判据 |
|---|---|---|
| **E1** | **运行时长可测** | 每个 run 的 `started_at` 非空；`elapsed_ms` 可从库中读出；运行时长分布可直接 `SELECT` |
| **E2** | **超时真正全局** | 一个 run 的累计存活时长不得超过 `timeout_seconds`，**与中断/恢复次数无关** |
| **E3** | **超时有部分结果** | 超时 run 的 `result` 非空，含已完成步骤与中断原因；不再是 NULL |
| **E4** | **能力可见性硬保证** | 装配结果中，Worker 声明的必得能力**100% 出现**；容量不足时**显式报错/告警**而非静默切片 |
| **E5** | **失败可信** | 外部/工具失败以**结构化失败**传播，模型收到的是可判别的失败对象；平台侧 `error` 事件覆盖全部失败路径 |
| **E6** | **结果一等** | `run.result` 是带类型的信封；结果卡可由 `card` 事件从历史**无损重放** |
| **E7** | **契约闭合** | 事件类型与 inbox 类型**单点定义**，发射/消费/冻结测试三方一致；未知类型有告警而非静默丢弃 |
| **E8** | **等待可观测** | 等待中的 run 在库中自描述（等谁、等多久、超时点），重启不误杀 |

### 3.2 不变量 I1–I6

原方案 I1–I5 继承，并补充 I6（失败语义）：

| 编号 | 不变量 | 反例 |
|---|---|---|
| I1 | **单次派发** | 让模型依次调 触发→查询→查询→取决策→取报告 |
| I2 | **状态外置** | 把"已解析 3/7 家"记在模型上下文里逐步推算 |
| I3 | **推送优先** | 让模型每 30 秒调一次状态查询 |
| I4 | **事实不经模型** | 模型自己写"平均降幅 12.3%" |
| I5 | **幂等贯穿** | 网络重连后回调重复触发导致重复落库 |
| **I6** | **失败不伪装**（新增） | 把 `HTTPError` 降级成一句中文返回给模型；工具失败时 `ok=True` |

### 3.3 明确不做（继承原方案 §0.3，锁定）

| 不新增 | 决定性依据 |
|---|---|
| 任务排队子系统（MQ） | `docs/设计方案.md` §1.4 明文不做算力调度与外部工作流引擎；§7.1 纪律② workers=1 |
| 资源预估 / 算力调度算法 | 平台不持有 GPU，该层无承载物 |
| Worker Pool 资源分配 | Worker = LangGraph 图，运行在平台自身 event loop 内 |
| 用户可调优先级 / 加急 | 无并发竞争对象 |
| Redis / RabbitMQ | 与"一库多角色"及 workers=1 纪律直接冲突 |
| 多用户权限体系 | `docs/设计方案.md` §1.4 明确不做 |

**判据纪律**：是否需要引入排队，唯一可靠指标是**任务时长分布**——而该指标在 Q-01 修复前**不可计算**。这是 v1.6 必须先把 Q-01 做掉的另一个理由。

---

## §4 P0 规格

> **P0 判据**：不做则流程跑不通，或结论不可信。
> 每项按「目标 / 现状证据 / 变更点 / DDL / 事件 schema / API / 迁移 / 测试 / 风险 / DoD」十段展开。
> **不新增子系统**：所有改造落在 `runs` / `run_events` / `inbox_events` / `task_steps` 及其必要扩展表上。

### P0-1 运行时长账本与真全局超时

**目标**：E1（时长可测）+ E2（超时全局）+ E3（超时保留部分结果）。

**现状证据**

- `[实测]` `runs.started_at` 27/27 为 NULL；全库无任何时长列（§2.1）。
- `[实测]` run `47d69dcc` 存活 1401s，最后一段恰好 600.004s 被杀，`result = NULL`（§2.2）。
- `[代码]` `runtime.py:446` `timeout = (run.budget or {}).get("timeout_seconds") or 600`；`runtime.py:~464` `asyncio.wait_for(self.graph.ainvoke(...), timeout=timeout)` —— **每次调用各起一次计时**。
- `[代码]` `_pause_for_confirmation`（`runtime.py:505-518`）只落 `status` + `budget_used`，**不落任何时间**。
- `[代码]` `_set_run_status`（`runtime.py:616-628`）只在终态写 `finished_at`。
- `[代码]` `runs/models.py:43-47` 只有 `started_at` / `finished_at`，无 deadline/elapsed。

**变更点**

1. **写入 `started_at`**：在 `_run` 首次置 `running` 的同一次提交内写 `started_at`（唯一执行入口，天然覆盖 `manual`/`timer`/`alarm` 三种 trigger）。不在 `conversations/service.py` 创建 run 时写——因为 `pending` 可能长时间排队，会把排队时间算进执行时长（`[推断]` 当前无排队，但语义先分清）。
2. **引入绝对截止时间 `deadline_at`**：`deadline_at = started_at + timeout_seconds`，`timeout_seconds` 取 **run 创建时固化的 `run.budget` 快照**，缺省回退 Agent 配置，再缺省回退 `settings` 默认 600。**一次写入、永不重置**——这直接消灭 Q-02。
3. **暂停顺延**：`_pause_for_confirmation` 记录 `paused_at`；恢复时 `deadline_at += (now - paused_at)`。用"绝对时间 + 显式顺延"而非"累加剩余秒数"，避免多次分段后的漂移与舍入误差。
4. **执行段剩余时间**：`_invoke_and_finalize` 改为
   `remaining = (deadline_at - now).total_seconds()`；
   `remaining <= 0` → **不调用图**，直接走超时收尾；否则 `wait_for(..., timeout=remaining)`。
5. **活跃时长 `active_ms`**：每次进入执行段记录段起点，在暂停/终态处累加到 `runs.active_ms`。`elapsed_ms` 不落列，由 `finished_at - started_at` 计算（避免冗余与不一致）。
6. **超时保留部分结果**：新增 `_finalize_timeout(run_id, phase)`，写
   `result = {"schema":"run_result/v1","partial":true,"reason":"timeout","text":<最近一段 AI 文本或占位>,"steps_done":[...],"budget_used":{...}}`，
   `error = {"code":"timeout","detail":"...","elapsed_ms":...,"active_ms":...,"deadline_at":...}`。
   **不新增 run 状态**（保持 `failed` + 结构化 `error.code`），避免状态机外溢——`RUN_STATUSES`（`runs/models.py:15-23`）与 `TERMINAL_RUN_STATUSES`（`runtime.py:44`）保持不变。

**DDL**

```sql
ALTER TABLE runs ADD COLUMN deadline_at  timestamptz;
ALTER TABLE runs ADD COLUMN active_ms    integer;
ALTER TABLE runs ADD COLUMN paused_at    timestamptz;
CREATE INDEX ix_runs_deadline_at ON runs (deadline_at);
```

**事件 schema**

- `run_status` payload 扩展（向后兼容，纯增字段）：
  `{"status":"running","elapsed_ms":int,"active_ms":int,"deadline_at":"iso"}`
- `error` payload 扩展：`{"code":"timeout","detail":str,"elapsed_ms":int,"active_ms":int,"partial_result":true}`

**API**

- `RunOut`（`runs/schemas.py`）新增 `deadline_at`、`active_ms`；`started_at` 已存在，从"永远 null"变为有值。
- 新增 `GET /runs/{run_id}` 计算字段 `elapsed_ms`（读取时由 `finished_at|now - started_at` 得出，不落库）。

**迁移**

- 三列可空，无破坏性变更。
- 存量 27 行可选回填：`UPDATE runs SET started_at = created_at WHERE started_at IS NULL;`——**必须标注为估算值**（`created_at` 是 run 创建时刻，非执行起点），仅用于让历史统计可用，不得用于 SLA 结论。

**测试**

| 类型 | 用例 |
|---|---|
| 单元 | `deadline_at` 计算：`started_at + timeout_seconds`；`budget.timeout_seconds` 缺省链路（run 快照 → agent → settings） |
| 单元 | 暂停顺延：模拟 2 次 `pause → resume`，断言 `deadline_at` 恰好顺延两次暂停时长，且 `active_ms` **不含**暂停时长 |
| 集成 | mock 图使 `ainvoke` 挂起 10s、`timeout_seconds=2` → 断言实际 `wait_for` 约 2s（**不是 10s**）、`status=failed`、`error.code=timeout`、`result` 非空且 `partial=true` |
| 集成 | 分段场景：2 次确认后第三次执行，断言**累计**时长不超 `timeout_seconds`（Q-02 回归） |
| 回归 | 新 run `started_at` / `deadline_at` 非空断言 |
| 冻结 | `test_engine_frozen.py` 不得因本项需要新增 `EVENT_TYPES` 条目 |

**风险**

| 风险 | 处置 |
|---|---|
| 长暂停（用户第二天才确认）使 `deadline_at` 大幅顺延、失去保护意义 | `deadline_at` 顺延的同时记录 `paused_ms` 并与 `settings.max_run_pause_seconds` 比较；超限则不再顺延，按"执行预算已耗尽"收尾并如实告知 |
| 存量 run 无 `deadline_at` | 读取处 `coalesce(deadline_at, started_at + 600s)` |
| 运行中改 Agent 配置导致预算漂移 | 一律以 run 创建时的 `budget` 快照为准（现有语义，本项不放松） |
| 时钟回拨 | 使用 DB `now()` 为单一时间源（`[代码]` 现网已用 `datetime.now(UTC)`；实施时统一到 DB 时间以避免多进程偏差） |

**DoD**

1. 新 run 的 `started_at` / `deadline_at` 非空率 100%。
2. 任意 run 的 `finished_at - started_at ≤ timeout_seconds + 单段收尾开销`，**与中断次数无关**。
3. 超时 run 的 `result` 非空且 `partial=true`。
4. §2.2 的尸检场景在测试中可重放，且不再出现超预算存活。

**实施记录（M9-b，已完成）**

| 项 | 落地形式 |
|---|---|
| 账本纯函数 | 新建 `backend/app/modules/engine/timing.py`：`resolve_timeout_seconds` / `compute_deadline` / `remaining_seconds` / `pause_extension` / `accumulate_active_ms` / `elapsed_ms` / `budget_metrics` / `partial_result`。**无 DB 依赖**，故可纯单测（仓库无 DB fixture 约定） |
| 列与索引 | `runs.deadline_at` / `active_ms` / `paused_at` + `ix_runs_deadline_at`；迁移 `a9b8c7d6e5f4`（`down_revision=g8d9e0f1a2b3`），双向可跑 |
| 配置 | `settings.run_default_timeout_seconds=600`、`settings.max_run_pause_seconds=3600`（替代硬编码 600） |
| 解析链 | `run.budget["timeout_seconds"]` → Agent 配置 → settings；**0/None 视为未配置**（现网三处写入方会把 `agent.timeout_seconds`（当前全 NULL）写进 budget，必须容忍） |
| 一次写入 | `started_at`/`deadline_at` 只在 `_set_run_status(..., "running")` 的**同一次提交**内写入，此后永不重置（消灭 Q-02） |
| 分段顺延 | `_pause_for_confirmation` 落 `paused_at` + 累加 `active_ms`；恢复时 `deadline_at += min(暂停时长, max_run_pause_seconds)`。**偏差说明**：风险表中"超限不再顺延并按预算耗尽收尾"改为**封顶顺延**——未完全顺延的差额通过 `deadline_at`/`elapsed_ms` 如实可见，不新增错误码（`active_ms` 已不含暂停时长） |
| 剩余时间超时 | `_invoke_and_finalize` 用 `remaining_seconds(deadline_at, now)`；`remaining ≤ 0` → **不调图**直接收尾（`phase="pre_invoke"`），否则 `wait_for(timeout=remaining)`（`phase="graph"`） |
| 超时收尾 | 新增 `_finalize_timeout`：从检查点取最后一段 AI 文本作为 `partial` 结果，`error={"code":"timeout","phase","retryable":true,"source":"engine","partial_result":true,...}`；**状态机不变**（`failed` + `error.code`），事件只发 `error` + `run_status` |
| 审计一致 | `_build_ctx` 的 `ctx.limits["timeout_seconds"]` 改用同一解析链（原为硬编码 600），audit 里看到的即实际生效的 |
| API | `RunOut` 增 `deadline_at` / `active_ms`；`elapsed_ms` 为**读取时 computed field**（不落库，遵守 A11）。`started_at` 由"永远 null"变为有值 |
| 存量数据 | `active_ms` 置 0、`started_at` **不回填**——`created_at` 是 run 创建时刻而非执行起点，回填会污染时长统计（规格中的"可选回填"按不可用于 SLA 结论放弃） |
| 时间源 | 沿用 `datetime.now(UTC)`（规格中"统一到 DB `now()`"**未做**，留待后续；单进程 workers=1 下无偏差风险） |
| 测试 | 新增 `tests/test_run_timeout.py`（11 例：解析链 / 暂停封顶 / active_ms 语义 / 信封结构 / deadline 一次写入 / 分段顺延不重置（Q-02 回归）/ 存量无 deadline 兜底 / 剩余 2s 而非 600s / 预算耗尽跳过图 / partial 结果与事件不扩散）。全量 `147 passed`（基线 130） |

**未做（明确留给后续项）**

- **done 路径仍写 `{"text": ...}`**，统一结果信封由 **P0-5** 落地（本项只先引入超时的 `partial` 信封）。
- DB `now()` 单一时间源、超时巡检任务（`deadline_at` 索引已就位，供后续巡检使用）。

---

### P0-2 能力可见性硬保证

**目标**：E4——Worker 声明的必得能力 100% 进入模型工具列表；容量不足时**显式告警**，不静默切片。

**现状证据**

- `[实测]` `filesystem` 单个 MCP 在 `capability_tools` 中展开 **14** 个工具；Agent `tool_budget` = 8 / 6（§2.3）。
- `[代码]` `assembler.py` 装配末尾为 `return {"tools": tools[:tool_budget] + _meta_tools(), "skills": skills}` —— **硬切片**。
- `[代码]` pinned 是 **Agent 级**（`retriever.py` `CapabilityBinding.mode == "pinned"`）；Worker 的 capabilities 只经 `_task_scope` → `domain_ids` 参与 `order_by_scope` **重排**（`retriever.py`），**无保留名额**。
- `[实测]` `capability_bindings` 仅 1 条 pinned，且没有任何 Worker 的领域能力被 pinned。
- `[推断]` `8 < 14`：只要 `filesystem` 进入候选且排序靠前，它单独即可吃满全部名额，Worker 业务能力被截断，**平台侧零事件、零日志、零提示**——这是"任务空转"的直接机制。

**变更点**

1. **引入「必得集」概念**（装配语义的一等概念，不是新子系统）：
   - Agent 级 `pinned` 绑定；
   - 当前 run 的 `_task_scope` 命中项（Worker/任务声明的 `domain_ids`）在 **Worker run 内**视为必得。
2. **装配改为分区**：
   ```text
   tools = 必得集(全量保留) + 共享区[:max(0, tool_budget - len(必得集))] + meta_tools
   ```
   `meta_tools` 维持现状**不占** `tool_budget`（`[代码]` 现实现即如此），但要在本文与 `模块详细设计.md` 中明确写死，避免日后歧义。
3. **容量不足的处理顺序**（明确优先级，杜绝静默）：
   1. 若 `len(必得集) > tool_budget` → **不做切片**，全量保留必得集，并发射 `capability_overflow`；
   2. 若 `len(必得集) > MAX_TOOLS_HARD`（新增硬上限，建议 24，与上下文窗口联动）→ **本 run 显式失败**，`error.code = "tool_capacity_exceeded"`，提示缩小 `domain_ids` 或提高预算。**宁可报错，不可静默截断**。
4. **可见性自检接口**：给定 `agent_id`（+ 可选 `conversation_id` / `task_id`），返回装配全貌：
   `{candidates[], required[], kept[], dropped[], tool_budget, hard_limit, overflow: bool, reason}`。
   用于 CI、运维与"为什么我的工具没出现"的一键诊断。
5. **Worker 声明落地校验**：注册/发布 Worker 时（`docs/第三方Worker开发与注册规范.md` 流程内）校验 `len(引用能力展开后的工具数) ≤ tool_budget`，不满足则**注册阶段就报错**，而不是等到运行时空转。
6. `retriever.py` 的 `_task_scope` 返回值增加显式标记（`required_ids`），由 `assemble_tools` 消费；`order_by_scope` 的分层排序**保持现状**（回归保护）。

**DDL**

无。纯装配逻辑改造。可选在 `runs.input` 记录 `capability_plan` 快照（便于事后归因），本项**不强制**。

**事件 schema**

新增事件类型 `capability_overflow`：

```json
{
  "reason": "tool_budget_insufficient",
  "tool_budget": 8,
  "required_count": 14,
  "kept": ["mcp__filesystem__read_file", "..."],
  "dropped": ["mcp__filesystem__write_file", "..."],
  "hard_limit": 24
}
```

> ⚠️ 新增事件类型**必须同步四处**：`backend.py:EVENT_TYPES`、`tests/test_engine_frozen.py:27` 的冻结断言、前端 `MessageStream.tsx` 分支、本文 §7 注册表。这是 P0-6 的纪律，P0-2 不得绕过。

**API**

- `GET /capabilities/visibility?agent_id={uuid}&conversation_id={uuid}` → 装配全貌。
  鉴权沿用 `capabilities/router.py:30` 的 router 级 `Depends(get_current_user)`（管理面语义）。

**迁移**

- 无 DDL。行为变更是"从切片变为保留必得集"，会让部分 Agent 的可见工具数上升（< 24 上限内），需在发布说明中标注。

**测试**

| 类型 | 用例 |
|---|---|
| 单元 | 构造 14 个 MCP 工具 + `tool_budget=8`，断言**必得集全部在结果内**、共享区按既有分层距离排序、`capability_overflow` 恰好发射一次 |
| 单元 | `tool_budget=6`、必得集 7 → 结果长度 ≥ 7（不切片），且事件中 `dropped` 非空 |
| 单元 | 必得集 25 > `MAX_TOOLS_HARD` → 本 run 失败且 `error.code=tool_capacity_exceeded` |
| 单元 | 无 pinned、无 `domain_ids` → 行为与改造前**完全一致**（回归） |
| 契约 | Worker 注册校验：引用能力展开后超预算 → 注册被拒 |
| 回归 | `tests/test_task_scope.py` 全部 6 条不得退化 |

**风险**

| 风险 | 处置 |
|---|---|
| 保留更多工具 → prompt 膨胀、成本上升 | 硬上限 + 超限显式失败；`active_ms`/token 双指标可在 P1 观察（§5） |
| `domain_ids` 变"必得"后，误判域会挤掉通用工具 | `capability_overflow` 事件使截断**可观测**；诊断接口可一键复核 |
| 部分 Agent `tool_budget` 配置过小（6）本身不合理 | 本项不擅自改配置值；由注册校验与自检接口把问题暴露给配置者 |

**DoD**

1. 对任意 `agent_id`，自检接口能列出装配全貌（候选/必得/保留/丢弃）。
2. 必得集丢失数 = 0；任何截断都有 `capability_overflow` 事件留痕。
3. `test_task_scope.py` 与装配相关既有测试全绿。

---

### P0-3 失败可信：结构化失败 + 禁止字符串降级 + 工具执行上下文

**目标**：E5——失败以结构化失败传播，模型可判别、平台可统计、用户可诊断；杜绝"假完成"。

**现状证据**

- `[代码]` `tools_builtin.py:171-190` `_procurement_request`：捕获 `httpx.HTTPError` 后**统一返回一句中文文本**（如"采购 Agent 服务不可用（…）"）。模型收到的是普通 `ToolMessage`，**无法区分"外部服务失败"与"业务结果就是这个字符串"** → 假完成根因。
- `[代码]` 同处 `except httpx.HTTPError` **不覆盖** JSON 解析失败 / schema 不匹配 / 业务非 2xx 语义错误；这些会冒泡成 run 级 `failed`。
- `[代码]` `graph.py` tools 节点 builtin 分支 `content, ok = await fn(dict(c["args"])), True` —— **`ok` 恒为 True**（元组右值就是字面量 `True`）。
- `[代码]` 同节点 mcp 分支 `ok = True` 亦为**硬编码**。
- `[代码]` `graph.py` verify 节点异常兜底为 `verdict = {"achieved": True, "feedback": ""}`（注释："验证失败视为达成，不阻断主链路"）——**验收失败反而记为达成**，第二个假完成入口。
- `[实测]` 全库 `run_events` 的 `error` 事件仅 **1** 条（§2.1）——失败面几乎不可观测。

**变更点**

1. **统一工具返回契约**（`ToolOutcome`）：
   ```json
   {"ok": true,  "content": <any>}
   {"ok": false, "error": {"code": "...", "detail": "...", "retryable": bool, "source": "builtin|mcp|external"}}
   ```
   builtin / mcp 两条通道**都**按此判定 `ok`，删除两处 `ok = True` 硬编码。对既有 builtin 函数（返回裸字符串/字典）保留适配层，逐步迁移，**适配层不得吞异常**。
2. **禁止字符串降级**：`_procurement_request` 不再把异常翻译成中文文本；改为返回/抛出结构化失败。**代码评审红线：`except` 分支不得产出面向模型的自然语言结果。**
3. **失败分类码表**（固定枚举，禁止自由文本）：
   | code | 含义 | retryable |
   |---|---|---|
   | `external_unavailable` | 连接失败 / 超时 | true |
   | `external_rejected` | 对端返回业务拒绝（4xx/业务码） | false |
   | `invalid_args` | 本地入参校验失败 | false（模型改参后可为 true） |
   | `parse_error` | 响应不可解析（JSON/schema） | true |
   | `internal_error` | 平台内部异常 | 视情 |
4. **失败进上下文（I6）**：失败以 `ToolMessage`（结构化失败）注入，而非自然语言；同时 `tool_result` 事件携带 `ok=false` 与 `error`。
5. **连续失败熔断**：同一 run 内连续 `N`（可配，建议 3）次 `external_unavailable`/`parse_error` → 结束 run，`error.code = "tool_failure_loop"`，**不得进入终答**。这是"假完成"的第二道闸。
6. **修正 verify 兜底语义**：异常时 `achieved = False`（或显式 `unknown` 并按"未达成"处理），并在 `protected_context.verify_verdict` 里标注 `verify_error`。**不允许**验收失败被记为达成。
7. **结果信封 outcome 字段**（与 P0-5 联动）：`result.outcome ∈ {done, partial, failed, blocked}`。

**DDL**

无（复用 `runs.error` JSONB 与 `run_events.payload`）。
可选（P1，本项不实施）：`tool_invocations` 审计表（run_id / tool / args_hash / ok / error_code / duration_ms / idempotency_key）。

**事件 schema**

- `tool_result` payload 扩展：`{"tool":str,"ok":bool,"error":{...}?,"result":...}`
- run 级失败沿用 `error` 事件，payload 固定为
  `{"code":str,"detail":str,"source":str,"retryable":bool,"phase":str}`

> 本项**不新增事件类型**，避免注册表扩散（Q-07 纪律）。

**API**

- `RunOut.error`（JSONB）在 `runs/schemas.py` 中给出显式结构（`code/detail/retryable/source`），前端不再靠字符串匹配展示失败。
- 前端失败卡：`error.code` → 用户可读文案 + 重试建议（`retryable` 决定是否给"重试"按钮）。

**迁移**

- 历史 run 的 `result` 中已存在的"中文降级文本"**不迁移**（不可逆信息丢失），但需在诊断文档中说明该现象的历史存在。
- `verify` 兜底语义变更是**行为变更**，需在发布说明中单独标注（预期失败率上升、但不再假完成）。

**测试**

| 类型 | 用例 |
|---|---|
| 单元 | monkeypatch 使 `httpx.AsyncClient` 抛 `ConnectError` → 断言 `_procurement_request` 返回**结构化失败**且不含中文自然语言 |
| 单元 | 对端返回非 JSON → 断言 `parse_error`（当前会冒泡成 run failed） |
| 单元 | builtin 工具返回失败契约 → 断言 `ToolMessage` 带 `ok=false`，且 tools 节点不改写 `ok` |
| 单元 | 连续 3 次 `external_unavailable` → `error.code = "tool_failure_loop"`，run 不进入终答 |
| 单元 | verify 抛异常 → `verify_verdict.achieved = False`（防假完成回归） |
| 集成 | 端到端"外部服务不可用"演练：断言 `result.outcome != "done"`、`error` 非空、无假完成文案 |
| 回归 | 现有 130 条测试全绿（重点：`test_discovery.py` 的 `test_normalize_*`） |

**风险**

| 风险 | 处置 |
|---|---|
| verify 语义收紧 → 正常 run 被误判未达成 → 重试增多 | 保留 `VERIFY_RETRY_LIMIT` 回环；异常与"未达成"分开标注；灰度观察 `error.code` 分布 |
| 适配层改造面大（builtin 函数多） | 分两步：先加契约与适配层（不改各函数），再逐函数迁移；每步独立 PR |
| 熔断阈值过紧影响长任务 | 阈值可配（Agent/Settings 级），默认 3 |

**DoD**

1. 平台内不存在"工具失败 → 自然语言结果"的路径（代码评审 + 单测双保证）。
2. 任意失败 run 的 `run.error` 结构完整（`code/detail/source/retryable`）。
3. `tool_result.ok=false` 可被前端与统计消费。
4. verify 异常不再产生 `achieved=true`。

---

### P0-4 await 一等化：外部等待成为平台能力

**目标**：E8——分钟级外部流程用"单次派发 + 平台等待"表达，不再由模型阻塞轮询；等待可观测、可重启恢复、幂等重放。

**现状证据**

- `[代码]` `tools_builtin.py:192-234`：`procurement_trigger/status/decisions/submit_decision/report` 五个工具构成**阻塞式轮询**形态（违反 I1）。
- `[代码]` `runtime.py:_reconcile_orphans`（94-108）把 `running` **一律**标 `failed`；`TERMINAL_RUN_STATUSES`（`runtime.py:44`）、`tasks/router.py:44` `NON_TERMINAL_RUN_STATUSES`、`runs/router.py` `confirm_run` 的 409 守卫、`_resume_run` 的状态守卫——**新增等待状态必须同步这 5 处**，否则启动即被误杀或确认被静默忽略。
- `[实测]` `inbox_events` 中 `resume` / `capability_changed` **从未写入**（§2.5）。
- `[代码]` `_reconcile_awaiting_steps`（`runtime.py:585-614`）→ `reconcile_orphaned_awaits`（`tasks/service.py`）已能把挂起的 `awaiting_user` 支线收敛为 `blocked`——**这是可复用的既有机制**，原方案称"等待不是一等能力"过重（§1.3 第 1 条）。
- `[实测]` 7 个 `blocked` 支线的 note 后缀"（存量清洗）"**在代码中不存在**（代码模板为 `tasks/service.py:578` 的"run 已结束，待确认支线自动收敛"）→ 现网靠**人工 SQL 清洗**（Q-15）。

**变更点**

1. **新增 `await_broker` 表**：等待成为**平台持有的状态**，不再是模型上下文里的轮询循环。
2. **工具契约改为单次派发**：派发类工具返回
   `{"status":"accepted","await_id":...}`（**一次调用即返回**）；平台据此把 run 置 `waiting_external` 并结束本执行段（`graph.ainvoke` 正常返回或 interrupt）。
3. **触发工具面简化**：`status` / `report` 轮询工具从**模型可见工具列表**中移除（能力仍保留供平台 watcher 调用）。这一条是 I1 落地的关键——**模型看不到轮询工具，就不可能轮询**。
4. **回调入口（拉取式）**：`POST /open/awaits/{await_id}/resolve`，外部服务**主动调用平台**。
   - **一期不做平台主动回调**：主动出网会引入 SSRF 面（外部可控 URL + 平台凭据），与 `docs/设计方案.md` §1.4 边界不符。若未来确需推送，目标 URL 必须来自能力 payload 中声明的白名单且强制 scheme/host 校验。
5. **双重鉴权**：`X-API-Key`（复用 `open_api/router.py:27` `verify_api_key`，与 `settings.open_api_token` 比对）+ `callback_token`（`await_id` 的 HMAC 派生，防止越权解他人 await）。
6. **幂等（I5）**：`await_broker.idempotency_key` **唯一约束**；重复回调返回同一结果、不产生二次副作用。
7. **CAS 收敛**：`UPDATE await_broker SET status='granted', payload_out=... WHERE id=:id AND status='waiting'`，`rowcount=0` → 说明已被处理/已过期，返回既有状态而非重放。并用 `UniqueConstraint` 保证同一 `task_step` 不会有两个活跃 await。
8. **超时与过期**：`await_broker.deadline_at`（独立于 run 执行预算，来自业务 SLA，默认 `settings.await_default_timeout_seconds`）；到期 → `status='expired'` → run 从 `waiting_external` 恢复，注入**结构化失败**（P0-3 契约，不是自然语言）。
9. **预算语义（必须写清，否则误判）**：
   - 等待**不计入** `active_ms`；等待期间平台不占用执行资源；
   - 等待期间**保留** run 的 `deadline_at` 顺延权（与 P0-1 第 3 条同一机制）；
   - `iterations` / `tool_calls` **不因等待增长**（一次派发 = 一次工具调用）。
10. **统一暂停路径**：把 `_pause_for_confirmation` 与新的 `_pause_for_await` 收敛为一个 `_pause(run_id, kind, payload)`，`kind ∈ {confirm, await}`，避免两套暂停逻辑漂移。
11. **状态机变更清单（实施时必须逐项打勾）**：
    | # | 位置 | 改动 |
    |---|---|---|
    | 1 | `runs/models.py:15-23` `RUN_STATUSES` | 加 `waiting_external` |
    | 2 | `runtime.py:44` `TERMINAL_RUN_STATUSES` | **不加**（等待非终态） |
    | 3 | `tasks/router.py:44` `NON_TERMINAL_RUN_STATUSES` | 加 `waiting_external` |
    | 4 | `runtime.py:_reconcile_orphans` | 保留列表加 `waiting_external`（**否则重启即误杀**） |
    | 5 | `runtime.py:_resume_run` 状态守卫 | 允许 `waiting_external` 被回调唤醒 |
    | 6 | `runs/router.py:confirm_run` 409 守卫 | 明确 `waiting_external` 不接受人工确认（返回明确提示而非 409 静默） |
    | 7 | `runtime.py:_reconcile_awaiting_steps` | 覆盖 `waiting_external` 终态收敛 |
    | 8 | 前端 `Chat/MessageStream.tsx` / `Chat/index.tsx` / `Runs/RunTimeline.tsx` | 新增等待态展示（含"等谁/等多久/超时点"） |
    | 9 | `tasks/service.py:render_task_card` | 等待态进任务卡 |

**DDL**

```sql
CREATE TABLE await_broker (
    id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    run_id            uuid NOT NULL REFERENCES runs(id) ON DELETE CASCADE,
    conversation_id   uuid REFERENCES conversations(id) ON DELETE CASCADE,
    task_id           uuid REFERENCES tasks(id) ON DELETE SET NULL,
    step_id           uuid REFERENCES task_steps(id) ON DELETE SET NULL,
    capability_id     uuid REFERENCES capabilities(id) ON DELETE SET NULL,
    tool_name         varchar(128) NOT NULL,
    idempotency_key   varchar(128) NOT NULL UNIQUE,
    callback_token    varchar(128) NOT NULL,          -- 派发时随请求下发给外部服务
    status            varchar(32) NOT NULL DEFAULT 'waiting',  -- waiting|granted|expired|cancelled
    deadline_at       timestamptz NOT NULL,
    payload_in        jsonb NOT NULL DEFAULT '{}'::jsonb,
    payload_out       jsonb,
    error             jsonb,
    attempts          integer NOT NULL DEFAULT 0,
    notified_at       timestamptz,
    resolved_at       timestamptz,
    created_at        timestamptz NOT NULL DEFAULT now(),
    updated_at        timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX ix_await_runs_status ON await_broker (run_id, status);
CREATE INDEX ix_await_deadline    ON await_broker (deadline_at) WHERE status = 'waiting';
-- 同一 run 的同一逻辑等待只能有一个活跃实例
CREATE UNIQUE INDEX uq_await_active ON await_broker (run_id, tool_name, idempotency_key);
```

**事件 schema**

新增 3 类（**必须同步注册表四处**，见 §7）：

| 事件 | payload |
|---|---|
| `await_started` | `{"await_id":..,"tool":..,"deadline_at":iso,"idempotency_key":..}` |
| `await_resolved` | `{"await_id":..,"status":"granted","payload":{...},"waited_ms":int}` |
| `await_expired` | `{"await_id":..,"waited_ms":int,"deadline_at":iso}` |

**API**

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| `POST` | `/open/awaits/{await_id}/resolve` | `X-API-Key` + `callback_token` | 外部服务回传结果；幂等 |
| `GET` | `/awaits?run_id=&status=` | 用户 JWT | 诊断：当前等待全貌 |
| `POST` | `/awaits/{await_id}/cancel` | 用户 JWT | 人工取消等待 |

**迁移**

- 纯新增表与状态，存量 run 无影响。
- `_reconcile_orphans` 的**保留列表是本次唯一"漏改即事故"的点**：必须在本项与 P0-1 的测试中显式覆盖"重启时 `waiting_external` 不被误杀"。

**测试**

| 类型 | 用例 |
|---|---|
| 单元 | 状态机：`waiting → granted` / `waiting → expired` / `waiting → cancelled`；非法转移被拒 |
| 单元 | 幂等：同一 `idempotency_key` 回调两次 → 只落一次结果、第二次返回既有结果、`attempts` 递增 |
| 单元 | CAS 竞争：并发两次 resolve → 仅一次 `rowcount=1` |
| 单元 | 超时：`deadline_at` 到达 → `expired` + run 恢复 + 注入结构化失败（`error.code="await_expired"`） |
| 单元 | 重启恢复：`_reconcile_orphans` 遇到 `waiting_external` **不标 failed** |
| 单元 | 幂等唯一索引：同一 `(run_id, tool, idempotency_key)` 插入两次 → 第二次被约束拒绝 |
| 集成 | 真实"派发 → 等待 → 外部回调"全流程：断言 run 从 `waiting_external` 恢复并 `done`、`waited_ms` 正确、`active_ms` **不含**等待时长 |
| 鉴权 | 错误 `X-API-Key` → 401；未配置 token → 503；错误 `callback_token` → 403 |
| 契约 | 模型工具列表**不含**轮询类工具（断言工具名不在装配结果中） |

**风险**

| 风险 | 处置 |
|---|---|
| 状态机扩展漏改一处 → 静默误杀/静默忽略 | 上表 9 项清单化为实施 checklist；每项配套测试 |
| 回调入口成为攻击面 | 双鉴权 + `callback_token` 绑定 `await_id` + 幂等 + 仅接受 JSON + 体积上限 |
| 外部服务永不回调导致资源滞留 | `deadline_at` 强制过期 + `GET /awaits` 可观测 |
| 等待期间用户改需求 | `POST /awaits/{id}/cancel` 显式取消，run 收到结构化"被取消"信号 |

**DoD**

1. 分钟级流程可用"一次派发 + 平台等待"表达，模型侧**零轮询**。
2. 等待在库中自描述（等谁、等到何时、等了多久），重启不误杀。
3. 重复回调不产生二次副作用。
4. 等待时长不计入 `active_ms`。

---

### P0-5 结果一等公民：结果信封 + 产物表 + 卡片事件 + 历史重放

**目标**：E6——结果是带类型的信封、可机读、可复用、可从历史无损重放；长结果不再被压缩链路折成一句话。

**现状证据**

- `[代码]` `runtime.py:494` `await self._finalize(run_id, "done", {"text": final_text})` —— 结果为**单字段文本**。
- `[代码]` `runtime.py:476` 计划被拒时 `result = {"text": "用户拒绝了执行计划，任务未执行。"}` —— 同为纯文本。
- `[实测]` `run_events` 无 `card` / `artifact` / `progress` 类型（§2.1）。
- `[代码]` 前端卡片仅由**确认事件**驱动：`CARD_REGISTRY`（`CardRenderer.tsx:461`）注册 4 类 + `GenericCard` 兜底（`:451`），`MessageStream.tsx:510` 处理 `confirmation_request`。**结果本身没有卡片通道**。
- `[代码]` `assembler.py:380` L1 压缩把最旧工具观察折成 `str(m.content)[:120]` —— **120 字符硬截断**；`COMPACT_L1_KEEP_RECENT = 4`（`:32`）。对分钟级业务的清单/报告类结果，折叠即失真。
- `[实测]` run `47d69dcc` 超时后 `result = NULL` —— 失败时结果**完全丢失**（与 P0-1 联动）。

**变更点**

1. **结果信封**（`schema` 版本化，向后兼容纯文本消费者）：
   ```json
   {
     "schema": "run_result/v1",
     "outcome": "done | partial | failed | blocked",
     "text": "终答文本（可能为空）",
     "cards": [{"card_type": "...", "payload": {...}}],
     "artifacts": [{"id": "uuid", "kind": "text|json|file|link", "name": "...", "mime": "...", "size": 1234}],
     "metrics": {"elapsed_ms": 0, "active_ms": 0, "iterations": 0, "tool_calls": 0, "input_tokens": 0, "output_tokens": 0},
     "reason": "timeout | rejected | tool_failure_loop | ... (可选)"
   }
   ```
   `metrics` 直接复用 `budget_used` + P0-1 的时长字段，**不新造账本**。
2. **新增 `run_artifacts` 表**：凡是"清单/报告/文件/大 JSON"，一律存表并以 `artifact_id` 引用；`result.text` 只放面向人的摘要。
3. **新增 `card` 事件**：结果卡与过程卡都走事件流，使卡片与事件序**对齐**，从而实现历史重放。
4. **历史重放**：前端新增 `card` 分支，从 `run_events` 重建卡片；`confirmation_request` 的内联卡片路径**保留**（兼容），但新卡片一律走 `card` 事件。
5. **压缩契约**（关键修正）：L1 折叠**不得作用于 artifact 引用**。折叠逻辑必须识别消息中的 artifact 引用并保留引用行（内容已在 DB，不在上下文）。同时把"结果必须先 artifact 化再进上下文"写成装配纪律。
6. **自动 artifact 化阈值**：单条工具观察 / 终答超 `settings.artifact_inline_max_chars`（建议 4000）→ 自动落 `run_artifacts`，上下文只留摘要 + 引用 id。
7. **失败也有结果**：与 P0-1 联动，超时/熔断 run 写 `outcome=partial|failed` 的信封 + `reason`，**不再 NULL**。

**DDL**

```sql
CREATE TABLE run_artifacts (
    id             uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    run_id         uuid NOT NULL REFERENCES runs(id) ON DELETE CASCADE,
    task_id        uuid REFERENCES tasks(id) ON DELETE SET NULL,
    step_id        uuid REFERENCES task_steps(id) ON DELETE SET NULL,
    kind           varchar(16) NOT NULL,        -- text|json|file|link
    name           varchar(256),
    mime           varchar(128),
    size           integer NOT NULL DEFAULT 0,
    storage        varchar(16) NOT NULL DEFAULT 'inline', -- inline|pg|file
    payload        jsonb,                       -- storage=inline/json
    file_id        uuid REFERENCES files(id) ON DELETE SET NULL,
    idempotency_key varchar(128) UNIQUE,
    created_at     timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX ix_artifacts_run ON run_artifacts (run_id);
CREATE INDEX ix_artifacts_task ON run_artifacts (task_id);
```

**事件 schema**

新增 `card`（**必须同步注册表四处**）：

```json
{"card_type": "result | plan_review | high_risk_tool | subtask_clarification | interactive_decision | <业务自定义>",
 "payload": {...},
 "artifact_id": "uuid|null",
 "seq_hint": 12}
```

`run_status` 终态 payload 增 `outcome` 与 `result_ref`（便于前端定位信封）。

**API**

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/runs/{run_id}/artifacts` | 列出产物 |
| `GET` | `/artifacts/{artifact_id}` | 取单条产物（`kind=file` 时重定向到 `files` 取件） |
| `GET` | `/runs/{run_id}/events`（既有） | 已可支撑卡片重放 |

前端：`CardRenderer.tsx` 的 `CARD_REGISTRY`（`:461`）新增 `result` 渲染器；`MessageStream.tsx` 新增 `card` 分支（与 `:510` 的 `confirmation_request` 并列）。

**迁移**

- 信封对旧数据**读取时 coalesce**：若 `result` 无 `schema` 字段，则按 `{"schema":"run_result/v0","text":result.text}` 解释。**不批量改历史**（不可逆且无收益）。
- 存量 27 个 run 的 `result` 不迁移。

**测试**

| 类型 | 用例 |
|---|---|
| 单元 | `_finalize` 产出信封：`schema` / `outcome` / `metrics` 字段齐全 |
| 单元 | 超阈值文本自动 artifact 化：断言 `run_artifacts` 落库 + `result.text` 为摘要 + 信封含 `artifact_id` |
| 单元 | L1 折叠遇到 artifact 引用 → **引用行保留**、内容不被截断（Q-05 核心回归） |
| 单元 | 旧格式 `{"text":...}` 读取 → `coalesce` 为 `v0` 信封且前端不报错 |
| 集成 | 卡片重放：从 `run_events` 重建卡片，断言与实时渲染结果一致 |
| 集成 | 超时 run → `result.outcome = partial` 且 `artifacts` 非空 |
| 回归 | `test_discovery.py` 中 `compact` / `normalize` 相关用例全绿 |

**风险**

| 风险 | 处置 |
|---|---|
| 信封是新契约，前端与第三方可能按旧结构解析 | `schema` 字段 + 读取时 coalesce + 一版兼容期 |
| artifact 表膨胀 | `size` 上限、`kind=file` 走既有 `files` 表、按 `run_id` 级联删除 |
| 卡片与既有 `confirmation_request` 双通道引发重复渲染 | 明确分工：确认卡继续走 `confirmation_request`，结果/过程卡走 `card`；前端去重键 = `(run_id, card_type, artifact_id)` |

**DoD**

1. 任意终态 run 的 `result` 非 NULL 且含 `schema` / `outcome` / `metrics`。
2. 长结果以 artifact + 摘要形式存在，**不再被 120 字符折叠**。
3. 结果卡可从历史事件无损重放。

---

### P0-6 契约闭合：事件注册表单点化 + inbox 类型闭合

**目标**：E7——事件类型与 inbox 类型**单点定义**，发射/消费/冻结测试三方一致；未知类型有告警而非静默丢弃。

**现状证据（三处漂移，§2.5）**

| 注册表 | 位置 | 条目数 | 漂移 |
|---|---|---|---|
| 后端冻结契约 | `backend.py:12-23` `EVENT_TYPES` | 10 | **缺 `context_compacted`**（`assembler.py:394`/`416`/`425` 在产线发射） |
| 实际发射点 | `runtime.py` / `graph.py` / `hooks_impl.py` / `assembler.py` | **11** | 注册表未覆盖 |
| 前端消费 | `Chat/MessageStream.tsx` / `Runs/RunTimeline.tsx` / `Chat/index.tsx` | **12** | `context_assembly`（`MessageStream.tsx:492`）、`interrupt`（`Runs/RunTimeline.tsx:139`）**无人发射** |
| inbox 声明 | `engine/models.py:20` `INBOX_EVENT_TYPES` | 5 | `resume` / `capability_changed` 从未落库 `[实测]` |
| inbox 实现 | `runtime.py:219-241` `_handle` | 3 | **无 `else` 兜底** |

冻结测试 `tests/test_engine_frozen.py:27` 断言的是 10 类硬编码集合——**它保护不了产线在用的 `context_compacted`**，所以漂移可以长期存活。

**变更点**

1. **事件注册表以 `EVENT_TYPES` 为唯一真源**（保留 `backend.py` 中的名字与位置，避免破坏既有 import 与"冻结接口"语义），补齐为：
   `message_delta` / `message_reset` / `thought` / `tool_call` / `tool_result` / `plan_updated` / `confirmation_request` / `budget_warning` / `run_status` / `error` / **`context_compacted`** / **`card`**（P0-5）/ **`capability_overflow`**（P0-2）/ **`await_started`** / **`await_resolved`** / **`await_expired`**（P0-4）。
2. **冻结测试改为派生式**：`test_engine_frozen.py:27` 不再硬编码 10 个字符串，改为断言"实际发射点集合 ⊆ `EVENT_TYPES`"（见下条），并保留一份**显式期望清单**便于评审时看到变更。
3. **新增 `tests/test_event_registry.py`**：AST 扫描 `backend/app` 中全部 `emit_event(...)` 与 `_emit(...)` 调用的**字符串字面量**，断言 `⊆ EVENT_TYPES`；发现未登记类型则失败。这是防止 §2.5 漂移再次发生的**唯一可靠手段**。
4. **inbox 声明与实现对齐**（二选一，本文选**收缩**）：
   - 将 `INBOX_EVENT_TYPES` 收缩为实际实现的 3 类（`user_input` / `confirmation` / `abort`）；
   - `capability_changed` 明确**不经 inbox**（走既有 PG 通知频道，`[代码]` `capabilities/service.py` 相关实现），在 `engine/models.py:20` 原地注释说明；
   - `resume` 若在 P0-4 中通过回调入口实现（`waiting_external → running`），则改为**经 inbox 投递 `resume`**并实现之；否则从声明中移除。**不得保留"声明了但没实现"的条目**。
5. **`_handle` 加 `else` 兜底**：未知类型 → `logger.warning` + 标记该 inbox 行为 `status='failed'`（或保留 `pending` 但记录 `attempts`），**绝不静默丢弃**。
6. **前端类型单点**：新增 `frontend/src/api/eventTypes.ts` 导出常量；删除永不命中的 `context_assembly`（`MessageStream.tsx:492`）与 `interrupt`（`Runs/RunTimeline.tsx:139`）死分支——**保留死分支会让后续维护者误判平台具备该能力**。
7. **本文 §7 注册表同步**：四张表（后端/发射点/前端/inbox）在 §7 中一次性登记，作为改动时的核对清单。

**DDL**：无。

**事件 schema**：本项只做**登记与一致性**，不新增 payload 语义。

**API**：无新增。可选：`GET /meta/event-types`（返回 `EVENT_TYPES`）便于前端与第三方对齐——**非必需**。

**迁移**

- 收缩 `INBOX_EVENT_TYPES` 是**契约变更**：若有外部系统按旧声明投递 `resume` / `capability_changed`，需在发布说明中标注（`[推断]` 当前无外部投递方，`[实测]` 这两类从未落库，风险低）。

**测试**

| 类型 | 用例 |
|---|---|
| 契约 | `test_event_registry.py`：发射点字面量 ⊆ `EVENT_TYPES` |
| 契约 | 冻结测试：显式期望清单与 `EVENT_TYPES` 一致 |
| 单元 | `_handle` 投递未知 inbox 类型 → 有日志、不静默丢弃、行为可查 |
| 单元 | `INBOX_EVENT_TYPES` 的每一项都能被 `_handle` 处理（参数化测试，逐项覆盖） |
| 前端 | 事件类型常量无死分支（lint 规则或测试断言：常量集合 = 分支集合） |

**风险**

| 风险 | 处置 |
|---|---|
| AST 扫描规则太脆（动态拼事件名） | 规则要求事件名为**字符串字面量**；发现动态拼名即视为违规（评审红线） |
| 新增事件类型需要四处同步，容易漏前端 | §7 表作为 checklist；前端未消费的事件**允许存在**（后端可先行），但必须在表内登记 |

**DoD**

1. `EVENT_TYPES` 覆盖全部发射点，且有自动化测试守护。
2. `INBOX_EVENT_TYPES` 中每一项都有实现与测试。
3. 未知 inbox 类型与未登记事件类型都能被发现（日志/测试）。

**实施记录（M9-b，已完成）**

| 项 | 落地形式 |
|---|---|
| 注册表 | `backend.py:EVENT_TYPES` 补 `context_compacted`（10 → **11** 类）。P0-2/P0-4/P0-5 的 `capability_overflow` / `await_*` / `card` **尚未登记**——按"登记即须发射"纪律（A10），与各自发射点同批加入 |
| 冻结测试 | `test_engine_frozen.py` 改为模块级显式 `EXPECTED_EVENT_TYPES`（含注释说明每类用途）与 `EVENT_TYPES` 双向一致断言，另加"无重复条目"断言 |
| 新守卫测试 | 新增 `backend/tests/test_event_registry.py`（6 例，AST + 正则，无 DB）：①发射点字面量 ⊆ `EVENT_TYPES`；②`EVENT_TYPES` 每项都有发射点（禁"声明了但没实现"）；③动态事件名发射点必须登记在 `_DYNAMIC_EMIT_SITES`（当前仅 `graph.py:build_graph.agent` 的 lambda 桥接，实际字面量在 `assembler.py`）；④前端 `eventTypes.ts` 注册表与后端一致；⑤前端无未登记的事件分支/图标条目；⑥`INBOX_EVENT_TYPES` 每项都有 `_handle` 分支 |
| inbox 收缩 | `INBOX_EVENT_TYPES` 由 5 类收缩为**实际实现的 3 类**（`user_input` / `abort` / `confirmation`）；`capability_changed`（走 PG 频道）与 `resume`（由 `confirmation` 载荷驱动）在原地注释说明并移出声明 |
| inbox 兜底 | `_handle` 增 `else`：`logger.warning` + 把该行置 `status='failed'`（认领时已置 `consumed`），**绝不静默丢弃** |
| 前端单点 | 新增 `frontend/src/api/eventTypes.ts`（`EVENT_TYPES` / `EventType` / `TRACE_EVENT_TYPES` / `isEventType`）；删除死分支 `context_assembly`（`MessageStream.tsx`）与 `interrupt`（`RunTimeline.tsx`），并删掉 `EVENT_META` 中同样无人发射的 `context_assembly` / `interrupt` / `budget_used` 三条死条目；`RunTimeline` 默认分支对未登记类型显式标注"未登记事件"而非静默渲染 |
| 验证 | `make lint` 通过；`uv run pytest -q` → **147 passed**；`frontend`: `npm run typecheck` 通过（注：`npm run lint` 因仓库未安装 `eslint` 依赖而不可用，属既有问题） |

**未做（明确留给后续项）**

- `GET /meta/event-types`（规格中标注为非必需）未做。
- 本节只做登记与一致性，未新增 payload 语义。

---

## §5 P1 规格（决定体验上限）

> P1 判据：流程已能跑通，但**不做得不到可用的体验**。
> P1 每项按「目标 / 现状证据 / 变更点 / 验收」四段展开。

### P1-1 有界并发与准入控制

**目标**：并发可观测、可控；超出容量时**明确告知**，不静默排队。

**现状证据**

- `[代码]` `runtime.py:245-246` `_spawn_run_task` → `asyncio.create_task(coro)`，**无信号量**。
- `[代码]` `runtime.py:224` 与 `:237` 分别从 inbox 的 `user_input` 与 `resume/confirmation` 路径触发 spawn。
- `[代码]` MCP 客户端为**单连接共享锁**（`mcp_client.py` 的 `self.lock`），长调用会串行化并拖慢健康巡检 `[推断]`。

**变更点**

1. 在 `_spawn_run_task` 前引入 `asyncio.Semaphore(settings.max_concurrent_runs)`（默认 4，可配）。
2. **不做排队**：超容时按 `[实测]` 现状给明确反馈——inbox 行保持 `pending` 并在 `run.input` 记录 `admitted:false`，同时向会话投递"当前有 N 个任务在执行"的可见提示（走既有 SSE 或消息通道）。
3. 并发度与 MCP 探活解耦：确保长 run 不阻塞 `mcp_pool` 的健康巡检（巡检任务独立，已有 `test_mcp_pool_selfheal.py` 覆盖）。

**验收**

1. 并发上限可配、可在运行时可观测（当前活跃 run 数）。
2. 超限时用户**立即**收到明确反馈，而非静默等待。
3. `tests/test_mcp_pool_selfheal.py` 15 条全绿。

### P1-2 配额前置化：默认值 + `provider.limits` 生效

**目标**：配额在**花费之前**生效；`limits` 字段从"留位"变为"语义"。

**现状证据**

- `[实测]` 两个 Agent 的 `max_tokens_per_run` 均为 NULL；`_build_ctx` 取 `int(budget.get("max_tokens_per_run") or 0)` → **0 = 不限**。
- `[代码]` `hooks_impl.py:171-175` token 闸在 `on_turn_end`（**已消耗完**）才判 → 事后熔断。
- `[代码]` `models_module/models.py:33` 有 `limits` 字段；消费点仅 `service.py:40`（写）与 `router.py:34`（读）；**无限流执行**。
- `[实测]` 9 个 provider `limits` 全为 `{}`（§2.1）。

**变更点**

1. `settings` / Agent 默认值：`max_tokens_per_run` 给出**有意义默认**（按 provider 上下文窗口与成本设定），不再允许"NULL = 不限"作为默认语义。
2. **前置闸**：在 `on_turn_start` 用**本轮 prompt token 估算 + 已用**预判是否会超限，超限则拒绝本轮并如实告知（不发起模型调用）。
3. **`limits` 消费**：模型调用处按 `provider.limits`（如 `rate_limit_rps` / `rate_limit_tpm`）实现令牌桶或滑动窗口；管理端补齐 `limits` 编辑表单（前端当前仅一行类型声明）。
4. `limits` 的**建议默认值**随迁移脚本给出（`[推断]` 需结合实际 provider 配额确定，不在本方案硬编码）。

**验收**

1. 配额耗尽时**不再发起**新的模型调用。
2. 配了 `limits` 即生效，且有测试证明限流触发。
3. 不再出现"钱花了才炸"。

### P1-3 记账口径修正

**目标**：`model_usage_daily` 的 token 与 run 数可比。

**现状证据**

- `[代码]` `hooks_impl.py:103-108` `on_run_end` 中 `if status != "done": return` —— **仅成功 run 计 `run_count`**，而 token 无论成败都在 `_upsert_usage` 累加。
- `[实测]` 可观测到"有 token 而无 run"的失真行 `[推断]`（原方案已指出 51,842 tokens / `run_count=0`）。

**变更点**

1. `run_count` 无论成败都计；若需区分，新增 `success_count` / `fail_count` 列（**优先不加列**，先用 `run_count` 全量口径 + 明细在 `runs` 表可查）。
2. 保持 `docs/设计方案.md` §8 统计口径一致性——若原口径是"只计成功"，需**同步修订文档**（二选一，不得代码与文档不一致）。

**验收**

1. 不存在"有 token 无 run"的新增行。
2. 文档口径与代码一致。

### P1-4 输入契约与预检门 ⏸（实施前须先核实）

**目标**：外部 Worker 的必需输入可声明、可预检，不依赖模型自觉。

**现状证据**

- ⏸ **本轮未复核**（§1.5）。原方案缺陷 C 称"front-matter 无处声明必需输入，校验依赖模型自觉"。
- `[代码]` 现存相关机制：`retriever.py:_task_scope`（`domain_ids` / `common_ids` / `hints`）、`tasks/service.py` 的 `detect_task_switch`（阈值 0.75）——说明"任务域"概念已存在，但**输入契约未见**。

**变更点（待核实后细化）**

1. **先做核实任务**：通读 `docs/第三方Worker开发与注册规范.md` + `workers/` 目录 front-matter 解析代码（`modules/workers`），确认是否已有必需输入声明机制。
2. 若无：在 Worker front-matter 增加 `inputs` 声明（名称/类型/必填/示例），注册时校验，装配时注入 `protected_context`。
3. **预检门**：必需输入缺失时**在调用模型之前**失败，并返回结构化"缺什么"（P0-3 契约），而不是让模型编造填满必填项。

**验收**

1. 未提供必需输入的任务在入模型前被拦截，错误信息含**缺失字段名**。
2. 模型上下文不再出现"编造值填必填项"的行为（可通过事件审计验证）。

### P1-5 推送式进度

**目标**：进度由平台 watcher 推进并推送，不由模型轮询。

**现状证据**

- `[代码]` `graph.py:275` 任务卡已带 `"progress": {"done": progress_done, "total": progress_total}` —— **进度存在但只在任务卡内**，不是事件。
- `[实测]` `run_events` **无 `progress` 类型**（§2.1）。

**变更点**

1. 进度成为事件：新增 `progress` 事件（**同步注册表四处**），payload `{"done":int,"total":int,"label":str}`。
2. 平台侧 watcher（worker 循环内）按 `await_broker` 与 `task_steps` 变化推进进度并发事件；前端 `MessageStream` / `TaskBoard` 消费。
3. 与 P0-4 的等待联动：等待期间进度来自外部回调，而非模型。

**验收**

1. 前端在**无需模型参与**的情况下看到进度前进。
2. 进度事件可从历史重放。

### P1-6 阻碍与决策的结构化可见

**目标**：`blocked` 支线有前台收敛路径，不再依赖人工 SQL。

**现状证据**

- `[实测]` `task_steps` 中 `blocked` 7 条（占 39%）；其 `resolution.note` 后缀"（存量清洗）"**在代码中不存在**（`tasks/service.py:578` 的模板为"run 已结束，待确认支线自动收敛"）→ 现网为**人工 SQL 清洗**（Q-15）。
- `[代码]` 自动收敛路径**已存在**：`_reconcile_awaiting_steps`（`runtime.py:585-614`）→ `reconcile_orphaned_awaits`（`tasks/service.py`），仅在 `failed/cancelled/aborted/timeout` 触发。
- `[实测]` `run_events` 无阻碍类事件。

**变更点**

1. 新增 `blocked` / `unblocked` 语义事件（或统一为 `step_status` 事件）→ 使阻碍**可观测、可推送**。
2. 前台收敛入口：任务卡上直接提供"关闭支线 / 重新排队 / 转人工"操作，落到 `task_steps.status` 与 `resolution`，**替代人工 SQL**。
3. 收敛原因枚举化：`run_ended` / `deadline_exceeded` / `user_cancelled` / `manual`，禁止自由文本后缀。

**验收**

1. 新增 `blocked` 支线只能由代码路径产生（不再出现人工后缀）。
2. 前台可完成收敛，且动作产生事件。

### P1-7 同轮 `search_more_tools` 缓存覆盖修复

**现状证据**

- `[代码]` `graph.py` tools 节点把**节点入口的** `state.get("capability_cache")` 传给 `run_search_more_tools`，而后者返回 `{"tools": tools}`（**整体 cache**）；`updated_cache` 为**单变量**——同轮两次搜索会互相覆盖。
- `[代码]` `assembler.py:run_search_more_tools` 的返回值语义未区分"增量"与"整体"。

**变更点**：把返回值改为**增量合并语义**（返回新增项），节点内按累积合并写回 `capability_cache`。

**验收**：单测：同轮连续两次 `search_more_tools` → 两次结果**都在**最终缓存中。

### P1-8 结果面与任务详情可同屏

**现状证据**

- `[代码]` `components/ContextPanel.tsx` 存在 `if (sidebar) return <PluginHost/>` 形态的**互斥替换**；`store/ui.ts` 把宽度提示硬夹到半屏；`sidebar` 为全局单例。

**变更点**：侧边栏与任务详情**并存**（分栏或可折叠）；宽度夹取上限放开为可配；明确**同时至多一个** plugin 的约束（保持单例，避免状态爆炸）。

**验收**：分钟级场景下"等解析"与"处理决策"可同屏查看。

### P1-9 run 级幂等键

**现状证据**：`[代码]` `conversations/service.py` 创建 run 无幂等键；`[推断]` 连点两次提交 = 两个 run = 双倍消耗。

**变更点**：会话提交时生成 `client_message_id`，`runs.input` 内落该键并加**部分唯一索引**（同会话内短窗口去重），重复提交返回同一 run。

**验收**：同一 `client_message_id` 提交两次 → 只有一个 run。

### P1-10 工具执行上下文（`run_id` / 幂等键可传递）

**现状证据**

- `[代码]` `graph.py` tools 节点：mcp 分支 `content = await mcp_pool.call_tool(UUID(meta["capability_id"]), meta["tool_name"], dict(c["args"]))` + `ok = True`；builtin 分支 `content, ok = await fn(dict(c["args"])), True` —— **两条通道都拿不到 `run_id` / 幂等键 / 回调凭据**。

**变更点**：工具调用协议扩展第 2 个参数（执行上下文：`run_id` / `task_id` / `step_id` / `idempotency_key` / `callback_token`），builtin 与 mcp 通道统一注入；对不接收上下文的既有 builtin 函数保留适配（**不吞异常**）。

**验收**：工具可读取 `run_id` 与幂等键；P0-4 的 `callback_token` 能经此下发。

---

## §6 P2 规格（规范与自检）

> P2 判据：不影响主流程正确性，但影响**可持续性**（边界、自检、可观测、文档一致）。

### P2-1 外部服务取件通道

**现状证据**：`[代码]` `files/router.py:19` 为 router 级 `Depends(get_current_user)`，`/upload`（`:33`）与 `/{file_id}/content`（`:52`）均要求用户 JWT → 无用户凭据的外部服务**无法取件**。

**变更点（边界优先）**

1. 新增 `/open/files/...` 组，复用 `open_api/router.py:27` 的 `verify_api_key`（`X-API-Key`）。
2. **授权范围最小化**：第三方只能取**自己任务产生的**文件——以 `run_artifacts` / `await_broker` 归属反查，禁止遍历式列举。
3. 仅 `GET`，且响应带 `Content-Disposition` 与体积上限。

**验收**：外部服务用 `X-API-Key` 能取到自己 run 的产物，取他人产物返回 404/403。

### P2-2 能力注册自检与冒烟强化

**现状证据**

- `[已修复]` `capabilities/smoke.py` 已用 `mcp_connectable(payload)` 跳过纯前端 plugin 的 MCP 冒烟，并有单测 `test_mcp_connectable_skips_frontend_only_plugin`（`tests/test_mcp_pool_selfheal.py`）——**原方案指出的缺陷不存在**。
- `[代码]` `capabilities/service.py:update_capability` 仅在 `description` / `payload` 非空时重建索引 → 改其他字段不重建（合理，但需测试固化）。

**变更点**

1. **回归保护**：为"纯前端 plugin 跳过冒烟"补显式回归测试标签（已在，纳入 §9 清单）。
2. 注册自检增强：注册时即校验"引用能力展开后的工具数 ≤ `tool_budget`"（与 P0-2 第 5 条同一校验，此处为文档同步）。
3. 索引重建条件写成测试（防回归）。

**验收**：`capabilities` 相关测试全绿；注册阶段能拦住"必然超预算"的 Worker。

### P2-3 阻碍收敛前台化（与 P1-6 配套的规范）

**变更点**：把 P1-6 的前台动作写入 `docs/模块详细设计.md` 与 `docs/前端设计.md`；人工 SQL 修复流程从 `docs/运维手册.md` 中**删除**（已被前台能力取代）。

**验收**：运维手册中不再存在"手工 UPDATE `task_steps`"的指引。

### P2-4 风险等级与确认疲劳

**现状证据**：⏸ `[未复核]` 原方案称内置 `filesystem` 能力 `risk_level=write`，导致**只读列举**也触发高危确认卡。

**变更点（核实后）**：`risk_level` 按**工具级**粒度评估（同名能力的不同工具风险不同），只读工具不得进高危门；否则确认疲劳会稀释真正的高危确认。

**验收**：只读工具不触发高危确认；高危工具仍触发。

### P2-5 文档同步清单

本方案落地会改变以下文档描述的事实，须**同批修订**：

| 文档 | 需修订内容 |
|---|---|
| `docs/数据库设计.md` | `runs` 新增列；新增 `await_broker` / `run_artifacts` 表 |
| `docs/模块详细设计.md` | 事件类型清单（§1.1.7 九类 → 实际清单）；inbox 类型清单；装配分区语义；暂停统一路径 |
| `docs/设计方案.md` | §11 演进接缝（`EVENT_TYPES` 变更需说明属于冻结接口的**受控扩展**） |
| `docs/前端设计.md` | `card` / `progress` / 等待态展示；侧边栏并存 |
| `docs/第三方Worker开发与注册规范.md` | 输入契约（P1-4）、派发/回调契约（P0-4）、取件通道（P2-1）、注册期预算校验（P0-2） |
| `docs/运维手册.md` | 时长/等待/失败码的查询方式；删除人工 SQL 收敛指引 |
| `docs/开发计划.md` | 追加 M9 里程碑与 tag `v1.6.0` |

### P2-6 可观测性面板

**变更点**：基于 P0-1 / P0-4 / P0-3 新增字段，提供三张只读视图：

1. 运行时长分布（`elapsed_ms` / `active_ms` P50 / P95）——**这才是判断"是否需要排队"的唯一可靠指标**（§3.3）；
2. 等待全貌（活跃 `await_broker`、等待时长、临近超时）；
3. 失败码分布（`runs.error.code` × 时间）。

**验收**：三个视图可直接 `SELECT` 得出，并作为发布节奏的决策输入。

---

## §7 契约总表

> **本节是实施期间的核对清单。** 任何新增事件 / 新状态 / 新表，先在此登记，再动代码。

### 7.1 事件类型注册表（必须三处一致）

> **本节由 `backend/tests/test_event_registry.py` 自动守护**：发射点 ⊆ `EVENT_TYPES`、
> `EVENT_TYPES` 每项都有发射点、前端 `frontend/src/api/eventTypes.ts` 与后端一致、前端无未登记分支。
> 下表最后两列是"自动校验"而非人工承诺。

| # | 事件 | 现状 | 后端 `EVENT_TYPES` | 前端 `eventTypes.ts` | 前端消费 | 来源 |
|---|---|---|---|---|---|---|
| 1 | `message_delta` | ✅ 在用（15996） | ✅ | ✅ | ✅ | `graph.py` |
| 2 | `message_reset` | ✅ 在用（29） | ✅ | ✅ | ✅ | `graph.py` |
| 3 | `thought` | ✅ 在用（54） | ✅ | ✅ | ✅ | `graph.py` |
| 4 | `tool_call` | ✅ 在用（98） | ✅ | ✅ | ✅ | `hooks_impl.py` |
| 5 | `tool_result` | ✅ 在用（98） | ✅ | ✅ | ✅ | `hooks_impl.py` |
| 6 | `plan_updated` | ✅ 在用（29） | ✅ | ✅ | ✅ | `graph.py` |
| 7 | `confirmation_request` | ✅ 在用（24） | ✅ | ✅ | ✅ | `runtime.py` |
| 8 | `budget_warning` | ❌ 从未触发 | ✅ | ✅ | ✅ | `hooks_impl.py` |
| 9 | `run_status` | ✅ 在用（102） | ✅ | ✅ | ✅ | `runtime.py` |
| 10 | `error` | ✅ 在用（1） | ✅ | ✅ | ✅ | `runtime.py` / `graph.py` |
| 11 | **`context_compacted`** | ✅ 在用（1） | ✅ **P0-6 已补** | ✅ | ✅ | `assembler.py:394/416/425` |
| — | ~~`context_assembly`~~ | ❌ 无人发射（**图节点名**） | ❌ | ❌ | ✅ **P0-6 已删** | — |
| — | ~~`interrupt`~~ | ❌ 无人发射 | ❌ | ❌ | ✅ **P0-6 已删** | — |
| — | **`card`** | 🆕 P0-5 | 待加 | 待加 | 待加 | `runtime.py` |
| — | **`capability_overflow`** | 🆕 P0-2 | 待加 | 待加 | 待加 | `assembler.py` |
| — | **`await_started`** | 🆕 P0-4 | 待加 | 待加 | 待加 | `runtime.py` |
| — | **`await_resolved`** | 🆕 P0-4 | 待加 | 待加 | 待加 | `runtime.py` |
| — | **`await_expired`** | 🆕 P0-4 | 待加 | 待加 | 待加 | `runtime.py` |
| — | **`progress`** | 🆕 P1-5 | 待加 | 待加 | 待加 | watcher |

**纪律**：①发射端必须用**字符串字面量**（动态事件名发射点必须登记到 `test_event_registry.py:_DYNAMIC_EMIT_SITES` 并写明理由）；②新事件必须同批更新 `backend.py:EVENT_TYPES`、`tests/test_engine_frozen.py:EXPECTED_EVENT_TYPES`、`frontend/src/api/eventTypes.ts`、前端消费分支、本表——漏一处即测试红灯；③前端未消费的事件允许存在，但必须在表中登记。

### 7.2 inbox 事件类型注册表

| # | 类型 | 现状声明（`engine/models.py:20`） | 现状实现（`runtime.py:219-241`） | v1.6 处置 |
|---|---|---|---|---|
| 1 | `user_input` | ✅ | ✅ | 保持 |
| 2 | `confirmation` | ✅ | ✅ | 保持 |
| 3 | `abort` | ✅ | ✅ | 保持 |
| — | ~~`resume`~~ | ❌ 已移出声明 | ❌ 不存在 | ✅ **P0-6 已移除**（恢复由 `confirmation` 载荷驱动；P0-4 若需独立投递再登记） |
| — | ~~`capability_changed`~~ | ❌ 已移出声明 | ❌ 不存在 | ✅ **P0-6 已移除**（走 PG `NOTIFY` 频道，原地注释说明） |
| — | 未知类型 | — | 曾**静默丢弃** | ✅ **P0-6 已加 `else` 兜底**：`logger.warning` + 该行置 `status='failed'` |

> 声明与实现的一致性由 `test_event_registry.py` 逐项参数化校验（`INBOX_EVENT_TYPES` 每项必须有 `_handle` 分支）。

### 7.3 run 状态机注册表

| 状态 | `RUN_STATUSES` | `TERMINAL_RUN_STATUSES`（`runtime.py:44`） | `NON_TERMINAL_RUN_STATUSES`（`tasks/router.py:44`） | `_reconcile_orphans` 保留 |
|---|---|---|---|---|
| `pending` | ✅ | — | ✅ | ✅ |
| `running` | ✅ | — | ✅ | ❌ **一律标 failed**（现状） |
| `paused_awaiting_confirm` | ✅ | — | ✅ | ✅ |
| **`waiting_external`** 🆕 P0-4 | 待加 | — | **待加** | **待加（漏改即事故）** |
| `done` | ✅ | ✅ | — | — |
| `failed` | ✅ | ✅ | — | — |
| `cancelled` | ✅ | ✅ | — | — |

**纪律**：`timeout` **不作为独立状态**（P0-1 用 `failed` + `error.code="timeout"`）；新增等待状态必须同批改：`runs/models.py:15-23`、`tasks/router.py:44`、`_reconcile_orphans`、`_resume_run` 守卫、`runs/router.py:confirm_run` 守卫、`_reconcile_awaiting_steps`、前端三处、`render_task_card`（清单见 P0-4 变更点 11）。

### 7.4 新增 DDL 汇总

| 表 / 列 | 用途 | 引入项 | 状态 |
|---|---|---|---|
| `runs.deadline_at` | 绝对截止时间（永不重置） | P0-1 | ✅ 已实施（迁移 `a9b8c7d6e5f4`） |
| `runs.active_ms` | 累计活跃执行时长（不含暂停/等待） | P0-1 | ✅ 已实施（迁移 `a9b8c7d6e5f4`） |
| `runs.paused_at` | 暂停起点（用于顺延） | P0-1 | ✅ 已实施（迁移 `a9b8c7d6e5f4`） |
| `ix_runs_deadline_at` | 超时巡检索引 | P0-1 | ✅ 已实施（迁移 `a9b8c7d6e5f4`） |
| `await_broker`（新表 + 3 索引） | 外部等待状态机 | P0-4 |
| `run_artifacts`（新表 + 2 索引） | 结果产物一等化 | P0-5 |

### 7.5 API 变更汇总

| 方法 | 路径 | 类型 | 鉴权 | 引入项 |
|---|---|---|---|---|
| `GET` | `/capabilities/visibility` | 新增 | 用户 JWT | P0-2 |
| `POST` | `/open/awaits/{id}/resolve` | 新增 | `X-API-Key` + `callback_token` | P0-4 |
| `GET` | `/awaits` | 新增 | 用户 JWT | P0-4 |
| `POST` | `/awaits/{id}/cancel` | 新增 | 用户 JWT | P0-4 |
| `GET` | `/runs/{id}/artifacts` | 新增 | 用户 JWT | P0-5 |
| `GET` | `/artifacts/{id}` | 新增 | 用户 JWT | P0-5 |
| `GET` | `/open/files/{id}/content` | 新增 | `X-API-Key`（最小授权） | P2-1 |
| — | `RunOut` 增 `deadline_at` / `active_ms` / `error` 结构 | 扩展 | — | P0-1 / P0-3 |
| — | `result` 信封 `run_result/v1` | 扩展 | — | P0-5 |

---

## §8 迁移与兼容

### 8.1 数据库迁移原则

1. **只加不改不删**：v1.6 全部 DDL 为 `ADD COLUMN`（可空）与 `CREATE TABLE`，无破坏性变更 → 可**先迁移后发版**，天然支持回滚（旧代码忽略新列）。
2. 新列在旧代码读路径上**必须有回退**：`deadline_at` 为 NULL 时按 `started_at + timeout_seconds` 兜底（`[推断]` 存量 27 行全为 NULL，必须覆盖）。
3. 新表**不写存量数据**：`await_broker` / `run_artifacts` 从零开始。

### 8.2 数据兼容矩阵

| 数据 | 兼容策略 | 依据 |
|---|---|---|
| 存量 `runs`（27 行）的 `result`（`{"text":...}`） | **读取时 coalesce** 为 `run_result/v0` 信封；不批量改写 | §P0-5 迁移 |
| 存量 `runs` 的 `started_at = NULL` | 可选回填 `created_at`，**标注为估算**，不得用于 SLA 结论 | §P0-1 迁移 |
| 存量 `blocked` 支线（7 条，含人工后缀） | 不做数据清洗；`resolution.note` 的自由后缀在新代码中不再产生（P1-6） | §2.1 / Q-15 |
| 存量"中文降级文本"结果 | 不迁移（不可逆），仅在诊断文档中说明历史现象 | §P0-3 迁移 |
| `INBOX_EVENT_TYPES` 收缩 | 契约变更；`[实测]` 这两类从未落库 → 风险低，仍需发布说明标注 | §P0-6 迁移 |

### 8.3 行为变更（需发布说明）

| 变更 | 用户可感知影响 |
|---|---|
| 超时按**全局**计算（不再分段重置） | 长流程更容易在总预算内被终止——但**保留部分结果**（净收益） |
| 必得能力不再被切片 | 部分 Agent 可见工具数上升（≤ 硬上限） |
| verify 异常不再记为"达成" | 失败率**上升**（更诚实）；假完成消失 |
| 工具失败不再变成自然语言 | 模型行为改变：会更早求助/失败，而非"完成" |
| 新增等待状态 | 前端需展示等待态（旧前端会把它当作未知状态） |
| 模型工具列表移除轮询工具 | 依赖轮询的既有 prompt/技能需同步修订 |

### 8.4 回滚策略

| 层级 | 回滚方式 |
|---|---|
| 代码 | 回退到 `v1.5.0` tag；新列/新表被旧代码忽略 → **无需 DB 回滚** |
| 状态机 | 若 `waiting_external` 需撤销：把存量该状态 run 批量置 `failed` + `error.code="rolled_back"`（有明确出口） |
| 行为开关 | 超时全局化、必得集保留、verify 严格化分别用 settings 开关，便于灰度（**默认开启**） |

---

## §9 验收体系

### 9.1 端到端验收（最高优先级）

| # | 场景 | 前置 | 断言 | 状态 |
|---|---|---|---|---|
| **V1** | **分段超时不重置** | mock 图在确认点中断两次，`timeout_seconds=30` | 总存活 ≤ 30s + 收尾开销；`error.code=timeout`；`result.outcome=partial` | ⏳ 账本语义已单测覆盖（`test_run_timeout.py`：deadline 一次写入 + 分段顺延不重置 + 用剩余而非全额）；**双次中断的真实链路复跑待 M9-c** |
| **V2** | **超时保留成果** | 图执行到产出中间产物后超时 | `result` 非空、`artifacts` 非空、`metrics` 完整 | ⏳ 部分结果已落（`text` + `metrics`）；`artifacts` 一等化属 **P0-5**，待其落地后补齐 |
| **V3** | **时长账本** | 任意正常 run | `started_at` / `deadline_at` 非空；`active_ms > 0`；`elapsed_ms ≥ active_ms` | ✅ 字段与计算字段已落地；真实 run 抽样待 M9-c（`GET /runs/{id}` 看 `deadline_at`/`active_ms`/`elapsed_ms`） |
| **V4** | **必得能力全可见** | 构造 14 工具 MCP + `tool_budget=8` 的 Agent | 必得集 100% 在装配结果内；`capability_overflow` 事件存在 |
| **V5** | **外部服务不可用 → 不假完成** | 派发工具指向不可达端点 | `result.outcome != "done"`；`error` 含 `external_unavailable`；无自然语言降级 |
| **V6** | **真实等待-回调** | 派发 → 平台进入 `waiting_external` → 外部回调 resolve | run 恢复并 `done`；`waited_ms` 正确；`active_ms` **不含**等待时长 |
| **V7** | **等待幂等** | 同一 `await_id` 回调两次 | 只落一次结果；第二次返回既有结果，无二次副作用 |
| **V8** | **等待超时** | 回调始终不发生，`deadline_at` 到达 | `await_broker.status=expired`；run 恢复并注入结构化失败 |
| **V9** | **重启不误杀等待** | `waiting_external` 期间重启引擎（含 `_reconcile_orphans` 执行） | 该 run **仍是** `waiting_external`（Q-07 核心回归） |
| **V10** | **模型零轮询** | 派发类任务的装配工具列表 | 轮询类工具名**不在**列表中 |
| **V11** | **结果重放** | 任一完成 run | 从 `run_events` 重建卡片与结果，与实时渲染一致 |
| **V12** | **L1 压缩不伤 artifact** | 长工具观察触发 L1 折叠 | artifact 引用保留；`result.text` 未被 120 字符截断影响 |
| **V13** | **契约一致性** | CI | 发射点 ⊆ `EVENT_TYPES`；`INBOX_EVENT_TYPES` 每项有实现 | ✅ `tests/test_event_registry.py` 6 例 |
| **V14** | **主机回归** | CI | `make lint`（ruff + `mypy app`）与 `uv run pytest` 全绿（基线 130 条不得减少） | ✅ 当前 `147 passed` |

### 9.2 功能验收清单（按 P0 项归口）

| 归口 | 验收点 |
|---|---|
| P0-1 | V1 / V2 / V3；`_set_run_status` 仍只在终态写 `finished_at`（不回退）。**已实施**：V3 ✅、V1/V2 单测覆盖待端到端复跑 |
| P0-2 | V4；`tests/test_task_scope.py` 6 条全绿 |
| P0-3 | V5；`verify` 异常 → `achieved=false`；连续失败熔断生效 |
| P0-4 | V6 / V7 / V8 / V9 / V10；等待状态 5 处同步点逐项验证 |
| P0-5 | V11 / V12；信封字段齐全 |
| P0-6 | V13；未知 inbox 类型有日志。**已实施** ✅ |
| P1 | P1-1…P1-10 各自的验收条 |
| P2 | P2-1…P2-6 各自的验收条 |

### 9.3 测试落位

| 测试文件 | 覆盖 |
|---|---|
| `tests/test_engine_frozen.py` | `EVENT_TYPES` 显式期望清单 |
| `tests/test_event_registry.py`（新增） | 发射点 ⊆ 注册表（AST 扫描） |
| `tests/test_run_timeout.py`（新增） | P0-1 全部用例 |
| `tests/test_capability_visibility.py`（新增） | P0-2 全部用例 |
| `tests/test_tool_failure.py`（新增） | P0-3 全部用例 |
| `tests/test_await_broker.py`（新增） | P0-4 状态机 / 幂等 / CAS / 超时 |
| `tests/test_run_artifacts.py`（新增） | P0-5 信封 / artifact / 折叠契约 |
| `tests/test_inbox_contract.py`（新增） | P0-6 inbox 逐项覆盖 |
| 既有 16 个文件 | **全部保持绿**（基线 130 条为下限） |

---

## §10 风险登记

| # | 风险 | 影响 | 缓解 | 归口 |
|---|---|---|---|---|
| R1 | 新等待状态漏改同步点 → 静默误杀 | run 被错误终止 | P0-4 的 9 项 checklist + V9 端到端回归 | P0-4 |
| R2 | verify 语义收紧导致失败率上升 | 用户体感变差 | 灰度开关 + `error.code` 分布观察 | P0-3 |
| R3 | 必得集保留导致 prompt 膨胀/成本上升 | 成本 | `MAX_TOOLS_HARD` + 显式失败 + 自检接口 | P0-2 |
| R4 | 回调入口成为攻击面 | 数据越权 | 双鉴权 + `callback_token` 绑 `await_id` + 幂等 + 体积上限 + 仅 JSON | P0-4 |
| R5 | 平台主动回调引入 SSRF | 内网探测 | **一期不做主动回调**；未来仅白名单 + scheme/host 校验 | P0-4 |
| R6 | AST 扫描规则过脆（动态事件名） | 契约守护失效 | 规则要求字面量；动态拼名视为评审红线 | P0-6 |
| R7 | 存量历史数据误导统计 | 决策错误 | 回填值标注"估算"；新指标从 v1.6 起算 | §8.2 |
| R8 | 一次性改动面过大（6 项 P0 并行） | 回归难定位 | 每项独立 PR + 独立测试文件；V14 作为每 PR 的门禁 | §12 |
| R9 | 外部服务无用户凭据仍无法取件（P2-1 未做前） | 业务闭环缺口 | P2-1 给最小授权通道；在此之前由调用方经平台工具代理取件 | P2-1 |
| R10 | 长时间暂停使 deadline 失去保护意义 | 预算失效 | 顺延上限 `max_run_pause_seconds` + 超限按耗尽收尾 | P0-1 |

---

## §11 反模式清单（违反即破坏本方案）

| # | 反模式 | 为什么破坏 |
|---|---|---|
| A1 | 在 `except` 分支把异常翻译成自然语言返回给模型 | 直接复活"假完成"（P0-3） |
| A2 | 新增 `run_events` 事件类型但不同步注册表四处 | 复活契约漂移（P0-6） |
| A3 | 用"剩余秒数累加"实现全局超时 | 多次分段后漂移；必须用绝对 `deadline_at`（P0-1） |
| A4 | 新增 run 状态但漏改 `_reconcile_orphans` | 重启即误杀（P0-4） |
| A5 | 让模型调用轮询工具"等"外部结果 | 回到阻塞轮询，撞执行预算（P0-4 / I1） |
| A6 | 静默切片工具列表 | 隐蔽的空转，最难排查（P0-2） |
| A7 | 把长结果内联进上下文（而非 artifact + 引用） | 触发 L1 折叠失真、成本上升（P0-5） |
| A8 | 为"等待"引入 Redis / MQ / 独立队列服务 | 违反 `docs/设计方案.md` §7.1 纪律②、一库多角色 |
| A9 | 用人工 SQL 收敛支线 | 不可持续、无审计（P1-6 / Q-15） |
| A10 | 保留"声明了但没实现"的契约条目 | 阅读代码者会误判能力存在（P0-6） |
| A11 | 把 `elapsed_ms` 同时落列与实时计算 | 双源不一致，必然分歧（P0-1） |
| A12 | 在 verify 异常时记为"达成" | 第二个假完成入口（P0-3） |

---

## §12 迭代计划（M9）

> 不含时间估算。每阶段结束打 tag，延续 `docs/开发计划.md` 的"纵切优先 + DoD + 每里程碑打 tag"惯例。

### M9-a：本方案定稿

**交付物**：`docs/优化方案-v1.6.md`（本文）。
**DoD**：事实审计的每个 `[代码]` 引用可定位；每个 `[实测]` 数字有复现命令；无越界内容（§3.3）。

### M9-b：P0 落地

| 顺序 | 项 | 依赖 | DoD |
|---|---|---|---|
| 1 | **P0-1** 时长账本与全局超时 ✅ **已实施** | — | V1 / V2 / V3 通过（V1/V2 由单测覆盖；真实链路复跑待 M9-c） |
| 2 | **P0-6** 契约闭合 ✅ **已实施** | — | V13 通过 |
| 3 | **P0-3** 失败可信 | 与 P0-1 共享收尾路径 | V5 通过 |
| 4 | **P0-5** 结果一等 | P0-1（失败也要有结果）、P0-6（`card` 事件） | V11 / V12 通过 |
| 5 | **P0-2** 能力可见性 | P0-6（`capability_overflow` 事件） | V4 通过 |
| 6 | **P0-4** await 一等化 | **P0-1（等待不得消耗执行预算）**、P0-3（超时注入结构化失败）、P0-6 | V6 / V7 / V8 / V9 / V10 通过 |

**顺序理由**：P0-6 与 P0-1 无依赖且是后续项的地基（新增事件类型与新的收尾路径）；P0-4 放在最后，因为它引入新状态且需要 P0-1 的时长语义与 P0-3 的失败契约才能正确表达"等待超时"。

**M9-b 出口 DoD**：V1–V14 全通过；`make lint` + `uv run pytest` 全绿；测试数 ≥ 130 + 新增用例。

### M9-c：P1 收口与端到端验收

1. P1-1 … P1-10 按 §5 顺序（P1-4 需先做核实任务）。
2. 真实业务链路演练：**一次完整的"派发 → 等待 → 回调 → 结果卡"** 端到端跑通（V6）。
3. **失败路径演练**：外部服务不可用、回调超时、重复回调三种异常各跑一次（V5 / V8 / V7）。
4. 打 tag **`v1.6.0`**；同步修订 §P2-5 列出的全部文档。

### M9-d：P2 规范与自检（可与 M9-c 并行）

P2-1 … P2-6；其中 **P2-3 的前台收敛能力依赖 P1-6**。

---

## §13 附录：证据复现命令

> 全部 `[实测]` 数字均由以下命令得出。**快照时间：2026-09-17**。
>
> ⚠️ **运行库是活库**：同一命令在不同时间执行会得到不同计数（复现时已观测到 `tasks.active` 由 9 变为 10）。
> 因此**结论只依赖结构性质**（`started_at` 全 NULL、`blocked` 占比、事件类型从缺、`limits` 全空、`tool_budget < 展开工具数`），
> 不依赖任何一个具体计数。引用数字时须带上快照时间。

### 13.1 数据面基线（§2.1）

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c "
select 'runs_status', status, count(*) from runs group by status
union all select 'runs_started_at_null', null, count(*) from runs where started_at is null
union all select 'runs_total', null, count(*) from runs
union all select 'tasks', status, count(*) from tasks group by status
union all select 'steps', status, count(*) from task_steps group by status
union all select 'caps', type, count(*) from capabilities group by type
union all select 'providers', kind, count(*) from model_providers group by kind
union all select 'bindings', mode, count(*) from capability_bindings group by mode
order by 1,2;"
```

### 13.2 事件类型分布（§2.1 / §2.5）

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c \
  "select event_type, count(*) from run_events group by 1 order by 2 desc;"
```

### 13.3 运行时长分布（§2.1，替代 `started_at` 路径）

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -c \
  "select round(extract(epoch from (finished_at - created_at))) as secs
   from runs where finished_at is not null order by secs desc;"
```

### 13.4 单 run 尸检（§2.2）

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c "
select status, error, result, budget_used,
       round(extract(epoch from (finished_at - created_at))) as alive_secs
from runs where id = '47d69dcc-8880-4848-860b-ab6e7d84d802';"
```

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c "
select created_at, event_type from run_events
where run_id = '47d69dcc-8880-4848-860b-ab6e7d84d802'
order by seq;"
```

### 13.5 能力展开与预算（§2.3）

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c "
select c.name, count(ct.tool_name) as expanded_tools
from capabilities c left join capability_tools ct on ct.capability_id = c.id
group by c.name order by expanded_tools desc;"
```

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c \
  "select name, tool_budget, max_iterations, max_tokens_per_run, timeout_seconds from agents;"
```

### 13.6 blocked 支线与人工后缀（§2.1 / Q-15）

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c \
  "select status, resolution from task_steps where status = 'blocked';"
```

对照代码模板：`backend/app/modules/tasks/service.py:578`（无"（存量清洗）"后缀）。

### 13.7 inbox 与 provider

```bash
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c \
  "select event_type, status, count(*) from inbox_events group by 1,2 order by 1;"
docker exec agent-platform-db psql -U agent -d agent_platform -t -A -F'|' -c \
  "select name, kind, status, limits from model_providers;"
```

### 13.8 代码事实与基线健康

```bash
cd backend && uv run pytest -q          # 基线：130 passed
make lint                               # ruff + mypy app
git log --oneline -1                    # 3fd07d0 (tag: v1.5.0)
```

| 结论 | 证据位置（HEAD `3fd07d0`） |
|---|---|
| `started_at` 从未写入 | `runs/models.py:46`（字段）+ `runtime.py:616-628`（仅终态写 `finished_at`） |
| 超时按段重置 | `runtime.py:446` + `runtime.py:~464` |
| 装配硬切片 | `discovery/assembler.py` `tools[:tool_budget] + _meta_tools()` |
| builtin 恒 `ok=True` | `engine/graph.py` tools 节点 builtin 分支 |
| HTTPError → 中文 | `engine/tools_builtin.py:171-190` |
| 事件注册表缺项 | `engine/backend.py:12-23` vs `discovery/assembler.py:394/416/425` |
| inbox 无 `else` | `engine/runtime.py:219-241`，声明见 `engine/models.py:20` |
| verify 异常记为达成 | `engine/graph.py` verify 节点 `except Exception` 分支 |
| 折叠 120 字符 | `discovery/assembler.py:380`，阈值 `:32`（`COMPACT_L1_KEEP_RECENT = 4`） |

---

**方案版本**：v1.6 / M9-a　·　**基线**：`3fd07d0`（v1.5.0）　·　**数据快照**：2026-09-17　·　**维护**：本文随 P0/P1/P2 实施进展更新，任何契约变更先在 §7 登记。
